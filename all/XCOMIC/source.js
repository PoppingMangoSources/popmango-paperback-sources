"use strict";
var _Sources = (() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIBinding.js
  var require_DUIBinding = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIBinding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIForm.js
  var require_DUIForm = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIForm.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIFormRow.js
  var require_DUIFormRow = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUIFormRow.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUISection.js
  var require_DUISection = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Exports/DUISection.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIButton.js
  var require_DUIButton = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIButton.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIHeader.js
  var require_DUIHeader = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIHeader.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIInputField.js
  var require_DUIInputField = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIInputField.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUILabel.js
  var require_DUILabel = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUILabel.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUILink.js
  var require_DUILink = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUILink.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIMultilineLabel.js
  var require_DUIMultilineLabel = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIMultilineLabel.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUINavigationButton.js
  var require_DUINavigationButton = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUINavigationButton.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIOAuthButton.js
  var require_DUIOAuthButton = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIOAuthButton.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISecureInputField.js
  var require_DUISecureInputField = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISecureInputField.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISelect.js
  var require_DUISelect = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISelect.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIStepper.js
  var require_DUIStepper = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUIStepper.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISwitch.js
  var require_DUISwitch = __commonJS({
    "node_modules/@paperback/types/lib/generated/DynamicUI/Rows/Exports/DUISwitch.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/ChapterDetails.js
  var require_ChapterDetails = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/ChapterDetails.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/Chapter.js
  var require_Chapter = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/Chapter.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/Cookie.js
  var require_Cookie = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/Cookie.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/HomeSection.js
  var require_HomeSection = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/HomeSection.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/IconText.js
  var require_IconText = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/IconText.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/MangaInfo.js
  var require_MangaInfo = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/MangaInfo.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/MangaProgress.js
  var require_MangaProgress = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/MangaProgress.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/PartialSourceManga.js
  var require_PartialSourceManga = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/PartialSourceManga.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/MangaUpdates.js
  var require_MangaUpdates = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/MangaUpdates.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/PBCanvas.js
  var require_PBCanvas = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/PBCanvas.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/PBImage.js
  var require_PBImage = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/PBImage.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/PagedResults.js
  var require_PagedResults = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/PagedResults.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/RawData.js
  var require_RawData = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/RawData.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/Request.js
  var require_Request = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/Request.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SourceInterceptor.js
  var require_SourceInterceptor = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SourceInterceptor.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/RequestManager.js
  var require_RequestManager = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/RequestManager.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/Response.js
  var require_Response = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/Response.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SearchField.js
  var require_SearchField = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SearchField.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SearchRequest.js
  var require_SearchRequest = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SearchRequest.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SourceCookieStore.js
  var require_SourceCookieStore = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SourceCookieStore.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SourceManga.js
  var require_SourceManga = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SourceManga.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SecureStateManager.js
  var require_SecureStateManager = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SecureStateManager.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/SourceStateManager.js
  var require_SourceStateManager = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/SourceStateManager.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/Tag.js
  var require_Tag = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/Tag.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/TagSection.js
  var require_TagSection = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/TagSection.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/TrackedMangaChapterReadAction.js
  var require_TrackedMangaChapterReadAction = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/TrackedMangaChapterReadAction.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/Exports/TrackerActionQueue.js
  var require_TrackerActionQueue = __commonJS({
    "node_modules/@paperback/types/lib/generated/Exports/TrackerActionQueue.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/generated/_exports.js
  var require_exports = __commonJS({
    "node_modules/@paperback/types/lib/generated/_exports.js"(exports) {
      "use strict";
      var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
          desc = { enumerable: true, get: function() {
            return m[k];
          } };
        }
        Object.defineProperty(o, k2, desc);
      } : function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        o[k2] = m[k];
      });
      var __exportStar = exports && exports.__exportStar || function(m, exports2) {
        for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
      };
      Object.defineProperty(exports, "__esModule", { value: true });
      __exportStar(require_DUIBinding(), exports);
      __exportStar(require_DUIForm(), exports);
      __exportStar(require_DUIFormRow(), exports);
      __exportStar(require_DUISection(), exports);
      __exportStar(require_DUIButton(), exports);
      __exportStar(require_DUIHeader(), exports);
      __exportStar(require_DUIInputField(), exports);
      __exportStar(require_DUILabel(), exports);
      __exportStar(require_DUILink(), exports);
      __exportStar(require_DUIMultilineLabel(), exports);
      __exportStar(require_DUINavigationButton(), exports);
      __exportStar(require_DUIOAuthButton(), exports);
      __exportStar(require_DUISecureInputField(), exports);
      __exportStar(require_DUISelect(), exports);
      __exportStar(require_DUIStepper(), exports);
      __exportStar(require_DUISwitch(), exports);
      __exportStar(require_ChapterDetails(), exports);
      __exportStar(require_Chapter(), exports);
      __exportStar(require_Cookie(), exports);
      __exportStar(require_HomeSection(), exports);
      __exportStar(require_IconText(), exports);
      __exportStar(require_MangaInfo(), exports);
      __exportStar(require_MangaProgress(), exports);
      __exportStar(require_PartialSourceManga(), exports);
      __exportStar(require_MangaUpdates(), exports);
      __exportStar(require_PBCanvas(), exports);
      __exportStar(require_PBImage(), exports);
      __exportStar(require_PagedResults(), exports);
      __exportStar(require_RawData(), exports);
      __exportStar(require_Request(), exports);
      __exportStar(require_SourceInterceptor(), exports);
      __exportStar(require_RequestManager(), exports);
      __exportStar(require_Response(), exports);
      __exportStar(require_SearchField(), exports);
      __exportStar(require_SearchRequest(), exports);
      __exportStar(require_SourceCookieStore(), exports);
      __exportStar(require_SourceManga(), exports);
      __exportStar(require_SecureStateManager(), exports);
      __exportStar(require_SourceStateManager(), exports);
      __exportStar(require_Tag(), exports);
      __exportStar(require_TagSection(), exports);
      __exportStar(require_TrackedMangaChapterReadAction(), exports);
      __exportStar(require_TrackerActionQueue(), exports);
    }
  });

  // node_modules/@paperback/types/lib/base/Source.js
  var require_Source = __commonJS({
    "node_modules/@paperback/types/lib/base/Source.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
      exports.urlEncodeObject = exports.convertTime = exports.Source = void 0;
      var Source = class {
        constructor(cheerio) {
          this.cheerio = cheerio;
        }
        /**
         * @deprecated use {@link Source.getSearchResults getSearchResults} instead
         */
        searchRequest(query, metadata) {
          return this.getSearchResults(query, metadata);
        }
        /**
         * @deprecated use {@link Source.getSearchTags} instead
         */
        async getTags() {
          return this.getSearchTags?.();
        }
      };
      exports.Source = Source;
      function convertTime(timeAgo) {
        let time;
        let trimmed = Number((/\d*/.exec(timeAgo) ?? [])[0]);
        trimmed = trimmed == 0 && timeAgo.includes("a") ? 1 : trimmed;
        if (timeAgo.includes("minutes")) {
          time = new Date(Date.now() - trimmed * 6e4);
        } else if (timeAgo.includes("hours")) {
          time = new Date(Date.now() - trimmed * 36e5);
        } else if (timeAgo.includes("days")) {
          time = new Date(Date.now() - trimmed * 864e5);
        } else if (timeAgo.includes("year") || timeAgo.includes("years")) {
          time = new Date(Date.now() - trimmed * 31556952e3);
        } else {
          time = new Date(Date.now());
        }
        return time;
      }
      exports.convertTime = convertTime;
      function urlEncodeObject(obj) {
        let ret = {};
        for (const entry of Object.entries(obj)) {
          ret[encodeURIComponent(entry[0])] = encodeURIComponent(entry[1]);
        }
        return ret;
      }
      exports.urlEncodeObject = urlEncodeObject;
    }
  });

  // node_modules/@paperback/types/lib/base/ByteArray.js
  var require_ByteArray = __commonJS({
    "node_modules/@paperback/types/lib/base/ByteArray.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/Badge.js
  var require_Badge = __commonJS({
    "node_modules/@paperback/types/lib/base/Badge.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
      exports.BadgeColor = void 0;
      var BadgeColor2;
      (function(BadgeColor3) {
        BadgeColor3["BLUE"] = "default";
        BadgeColor3["GREEN"] = "success";
        BadgeColor3["GREY"] = "info";
        BadgeColor3["YELLOW"] = "warning";
        BadgeColor3["RED"] = "danger";
      })(BadgeColor2 = exports.BadgeColor || (exports.BadgeColor = {}));
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/ChapterProviding.js
  var require_ChapterProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/ChapterProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/CloudflareBypassRequestProviding.js
  var require_CloudflareBypassRequestProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/CloudflareBypassRequestProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/HomePageSectionsProviding.js
  var require_HomePageSectionsProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/HomePageSectionsProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/MangaProgressProviding.js
  var require_MangaProgressProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/MangaProgressProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/MangaProviding.js
  var require_MangaProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/MangaProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/RequestManagerProviding.js
  var require_RequestManagerProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/RequestManagerProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/SearchResultsProviding.js
  var require_SearchResultsProviding = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/SearchResultsProviding.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/interfaces/index.js
  var require_interfaces = __commonJS({
    "node_modules/@paperback/types/lib/base/interfaces/index.js"(exports) {
      "use strict";
      var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
          desc = { enumerable: true, get: function() {
            return m[k];
          } };
        }
        Object.defineProperty(o, k2, desc);
      } : function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        o[k2] = m[k];
      });
      var __exportStar = exports && exports.__exportStar || function(m, exports2) {
        for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
      };
      Object.defineProperty(exports, "__esModule", { value: true });
      __exportStar(require_ChapterProviding(), exports);
      __exportStar(require_CloudflareBypassRequestProviding(), exports);
      __exportStar(require_HomePageSectionsProviding(), exports);
      __exportStar(require_MangaProgressProviding(), exports);
      __exportStar(require_MangaProviding(), exports);
      __exportStar(require_RequestManagerProviding(), exports);
      __exportStar(require_SearchResultsProviding(), exports);
    }
  });

  // node_modules/@paperback/types/lib/base/SourceInfo.js
  var require_SourceInfo = __commonJS({
    "node_modules/@paperback/types/lib/base/SourceInfo.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
      exports.ContentRating = exports.SourceIntents = void 0;
      var SourceIntents2;
      (function(SourceIntents3) {
        SourceIntents3[SourceIntents3["MANGA_CHAPTERS"] = 1] = "MANGA_CHAPTERS";
        SourceIntents3[SourceIntents3["MANGA_TRACKING"] = 2] = "MANGA_TRACKING";
        SourceIntents3[SourceIntents3["HOMEPAGE_SECTIONS"] = 4] = "HOMEPAGE_SECTIONS";
        SourceIntents3[SourceIntents3["COLLECTION_MANAGEMENT"] = 8] = "COLLECTION_MANAGEMENT";
        SourceIntents3[SourceIntents3["CLOUDFLARE_BYPASS_REQUIRED"] = 16] = "CLOUDFLARE_BYPASS_REQUIRED";
        SourceIntents3[SourceIntents3["SETTINGS_UI"] = 32] = "SETTINGS_UI";
      })(SourceIntents2 = exports.SourceIntents || (exports.SourceIntents = {}));
      var ContentRating2;
      (function(ContentRating3) {
        ContentRating3["EVERYONE"] = "EVERYONE";
        ContentRating3["MATURE"] = "MATURE";
        ContentRating3["ADULT"] = "ADULT";
      })(ContentRating2 = exports.ContentRating || (exports.ContentRating = {}));
    }
  });

  // node_modules/@paperback/types/lib/base/HomeSectionType.js
  var require_HomeSectionType = __commonJS({
    "node_modules/@paperback/types/lib/base/HomeSectionType.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
      exports.HomeSectionType = void 0;
      var HomeSectionType2;
      (function(HomeSectionType3) {
        HomeSectionType3["singleRowNormal"] = "singleRowNormal";
        HomeSectionType3["singleRowLarge"] = "singleRowLarge";
        HomeSectionType3["doubleRow"] = "doubleRow";
        HomeSectionType3["featured"] = "featured";
      })(HomeSectionType2 = exports.HomeSectionType || (exports.HomeSectionType = {}));
    }
  });

  // node_modules/@paperback/types/lib/base/PaperbackExtensionBase.js
  var require_PaperbackExtensionBase = __commonJS({
    "node_modules/@paperback/types/lib/base/PaperbackExtensionBase.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/base/index.js
  var require_base = __commonJS({
    "node_modules/@paperback/types/lib/base/index.js"(exports) {
      "use strict";
      var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
          desc = { enumerable: true, get: function() {
            return m[k];
          } };
        }
        Object.defineProperty(o, k2, desc);
      } : function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        o[k2] = m[k];
      });
      var __exportStar = exports && exports.__exportStar || function(m, exports2) {
        for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
      };
      Object.defineProperty(exports, "__esModule", { value: true });
      __exportStar(require_Source(), exports);
      __exportStar(require_ByteArray(), exports);
      __exportStar(require_Badge(), exports);
      __exportStar(require_interfaces(), exports);
      __exportStar(require_SourceInfo(), exports);
      __exportStar(require_HomeSectionType(), exports);
      __exportStar(require_PaperbackExtensionBase(), exports);
    }
  });

  // node_modules/@paperback/types/lib/compat/DyamicUI.js
  var require_DyamicUI = __commonJS({
    "node_modules/@paperback/types/lib/compat/DyamicUI.js"(exports) {
      "use strict";
      Object.defineProperty(exports, "__esModule", { value: true });
    }
  });

  // node_modules/@paperback/types/lib/index.js
  var require_lib = __commonJS({
    "node_modules/@paperback/types/lib/index.js"(exports) {
      "use strict";
      var __createBinding = exports && exports.__createBinding || (Object.create ? function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        var desc = Object.getOwnPropertyDescriptor(m, k);
        if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
          desc = { enumerable: true, get: function() {
            return m[k];
          } };
        }
        Object.defineProperty(o, k2, desc);
      } : function(o, m, k, k2) {
        if (k2 === void 0) k2 = k;
        o[k2] = m[k];
      });
      var __exportStar = exports && exports.__exportStar || function(m, exports2) {
        for (var p in m) if (p !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p)) __createBinding(exports2, m, p);
      };
      Object.defineProperty(exports, "__esModule", { value: true });
      __exportStar(require_exports(), exports);
      __exportStar(require_base(), exports);
      __exportStar(require_DyamicUI(), exports);
    }
  });

  // src/XCOMIC/XCOMIC.ts
  var XCOMIC_exports = {};
  __export(XCOMIC_exports, {
    XCOMIC: () => XCOMIC,
    XCOMICInfo: () => XCOMICInfo
  });

  // common/UrlBuilder.ts
  function hostOf(url) {
    return url.match(/^[a-z][a-z0-9+.-]*:\/\/([^/:?#]+)/i)?.[1] ?? "";
  }

  // common/Cloudflare.ts
  var CloudflareError = class extends Error {
    constructor(request) {
      super(
        `CLOUDFLARE BYPASS ERROR:
Please go to the homepage of <${hostOf(request.url)}> and press the cloud icon.`
      );
      this.name = "CloudflareError";
      this.request = request;
    }
  };
  var CHALLENGE_HEADER = "cf-mitigated";
  var CHALLENGE_BODY = /Just a moment|cf-chl-|_cf_chl_opt|cf_chl_prog|g-recaptcha|challenge-platform/i;
  function isCloudflareChallenge(status, headers, body) {
    const mitigated = headerValue(headers, CHALLENGE_HEADER);
    if (mitigated?.toLowerCase() === "challenge") {
      return true;
    }
    if (status !== 403 && status !== 503) {
      return false;
    }
    const server = headerValue(headers, "server")?.toLowerCase() ?? "";
    return CHALLENGE_BODY.test(body) || server.includes("cloudflare");
  }
  function headerValue(headers, name) {
    if (headers === void 0) {
      return void 0;
    }
    const wanted = name.toLowerCase();
    for (const key of Object.keys(headers)) {
      if (key.toLowerCase() === wanted) {
        const value = headers[key];
        return typeof value === "string" ? value : void 0;
      }
    }
    return void 0;
  }

  // common/Dates.ts
  var UNIT_MS = {
    second: 1e3,
    minute: 60 * 1e3,
    hour: 60 * 60 * 1e3,
    day: 24 * 60 * 60 * 1e3,
    week: 7 * 24 * 60 * 60 * 1e3,
    month: 30 * 24 * 60 * 60 * 1e3,
    year: 365 * 24 * 60 * 60 * 1e3
  };

  // common/Info.ts
  var import_types = __toESM(require_lib());
  var INTENT_FOR = {
    ["chapters" /* CHAPTERS */]: import_types.SourceIntents.MANGA_CHAPTERS,
    ["homePage" /* HOME_PAGE */]: import_types.SourceIntents.HOMEPAGE_SECTIONS,
    ["cloudflare" /* CLOUDFLARE */]: import_types.SourceIntents.CLOUDFLARE_BYPASS_REQUIRED,
    ["settings" /* SETTINGS */]: import_types.SourceIntents.SETTINGS_UI,
    ["tracking" /* TRACKING */]: import_types.SourceIntents.MANGA_TRACKING
  };
  function sourceInfo(description) {
    const intents = description.capabilities.reduce(
      (mask, capability) => mask | INTENT_FOR[capability],
      0
    );
    const badges = [...description.badges ?? []];
    if (description.kind === "Novels" /* NOVEL */) {
      badges.unshift({ text: "Novels" /* NOVEL */, type: import_types.BadgeColor.BLUE });
    }
    if (description.language !== void 0) {
      badges.unshift({ text: description.language, type: import_types.BadgeColor.GREY });
    }
    return {
      name: description.name,
      description: description.description,
      version: description.version,
      icon: description.icon,
      author: "Popmango",
      authorWebsite: "https://github.com/PoppingMangoSources",
      websiteBaseURL: description.websiteBaseURL,
      contentRating: runtimeRating(description.contentRating),
      language: description.language,
      sourceTags: badges,
      intents
    };
  }
  function runtimeRating(rating) {
    switch (rating) {
      case "ADULT" /* ADULT */:
        return import_types.ContentRating.ADULT;
      case "MATURE" /* MATURE */:
        return import_types.ContentRating.MATURE;
      case "EVERYONE" /* EVERYONE */:
        return import_types.ContentRating.EVERYONE;
    }
  }

  // common/Interceptor.ts
  var IMAGE_URL = /\.(avif|bmp|gif|jpe?g|jxl|png|svg|webp)(?:[/?#]|$)/i;
  var PaperbackInterceptor = class {
    /** Adjusts a request before it leaves — headers, cookies, rewritten URLs. */
    async interceptRequest(request) {
      return request;
    }
    /**
     * Inspects or rewrites a response before the source sees it.
     *
     * This is where a source detects a bot challenge and throws, or unpacks a
     * body the site has obfuscated.
     */
    async interceptResponse(_request, _response, body) {
      return body;
    }
  };
  var BasicRateLimiter = class {
    constructor(options) {
      /** Timestamps of the requests still inside the current window. */
      this.recent = [];
      /** Serialises callers so two of them cannot claim the same slot. */
      this.queue = Promise.resolve();
      this.numberOfRequests = Math.max(1, options.numberOfRequests);
      this.bufferInterval = Math.max(0, options.bufferInterval);
      this.ignoreImages = options.ignoreImages ?? false;
    }
    /** Resolves once the caller is allowed to send its request. */
    async wait(url) {
      if (this.bufferInterval === 0 || this.ignoreImages && IMAGE_URL.test(url)) {
        return;
      }
      const turn = this.queue.then(() => this.claimSlot());
      this.queue = turn.catch(() => void 0);
      return turn;
    }
    async claimSlot() {
      const windowMs = this.bufferInterval * 1e3;
      const now = Date.now();
      this.recent = this.recent.filter((at) => now - at < windowMs);
      if (this.recent.length >= this.numberOfRequests) {
        const oldest = this.recent[0] ?? now;
        const waitFor = windowMs - (now - oldest);
        if (waitFor > 0) {
          await new Promise((resolve) => setTimeout(resolve, waitFor));
        }
        this.recent = this.recent.filter((at) => Date.now() - at < windowMs);
      }
      this.recent.push(Date.now());
    }
  };
  var CookieStorageInterceptor = class {
    constructor(stateManager, key = "stored_cookies") {
      this.cookies = {};
      this.stateManager = stateManager;
      this.key = key;
    }
    /** Stores a cookie, dropping it instead if it has already expired. */
    setCookie(cookie) {
      if (cookie.expires !== void 0 && cookie.expires.getTime() <= Date.now()) {
        delete this.cookies[cookie.name];
      } else {
        this.cookies[cookie.name] = cookie.value;
      }
      void this.persist();
    }
    /** Forgets every stored cookie. */
    clear() {
      this.cookies = {};
      void this.persist();
    }
    /**
     * Returns the stored cookies, loading them from state on first use.
     *
     * The load is shared rather than merely flagged. The home page fires every
     * section at once on a cold start, so without this each of them would race
     * past a half-set flag and go out with no cookies — one request would
     * carry the saved clearance and the rest would come back challenged.
     */
    async all() {
      this.hydration ??= this.hydrate();
      await this.hydration;
      return this.cookies;
    }
    async hydrate() {
      try {
        const stored = await this.stateManager.retrieve(this.key);
        this.cookies = { ...stored ?? {}, ...this.cookies };
      } catch {
      }
    }
    async persist() {
      try {
        await this.stateManager.store(this.key, this.cookies);
      } catch {
      }
    }
  };
  var InterceptorChain = class {
    constructor(options) {
      this.rateLimiter = options.rateLimiter;
      this.cookieStorage = options.cookieStorage;
      this.interceptor = options.interceptor;
    }
    async interceptRequest(request) {
      await this.rateLimiter?.wait(request.url);
      let intercepted = {
        url: request.url,
        method: request.method,
        headers: { ...request.headers },
        cookies: {
          ...await this.cookieStorage?.all(),
          ...cookiesToRecord(request.cookies)
        },
        data: request.data,
        param: request.param
      };
      if (this.interceptor !== void 0) {
        intercepted = await this.interceptor.interceptRequest(intercepted);
      }
      return App.createRequest({
        url: intercepted.url,
        method: intercepted.method,
        headers: intercepted.headers,
        param: intercepted.param,
        data: intercepted.data,
        cookies: recordToCookies(intercepted.cookies, hostOf(intercepted.url))
      });
    }
    async interceptResponse(response) {
      if (this.interceptor === void 0) {
        return response;
      }
      const request = {
        url: response.request.url,
        method: response.request.method,
        headers: { ...response.request.headers },
        cookies: cookiesToRecord(response.request.cookies),
        data: response.request.data,
        param: response.request.param
      };
      const body = await this.interceptor.interceptResponse(request, response, {
        text: response.data ?? "",
        raw: response.rawData,
        contentType: headerValue(response.headers, "content-type")
      });
      const contentTypeChanged = body.contentType !== void 0 && body.contentType !== headerValue(response.headers, "content-type");
      if (body.text === (response.data ?? "") && body.raw === response.rawData && !contentTypeChanged) {
        return response;
      }
      try {
        const mutable = response;
        mutable.data = body.text;
        mutable.rawData = body.raw;
        if (contentTypeChanged) {
          mutable.mimeType = body.contentType;
          setContentType(response.headers, body.contentType ?? "");
        }
        return response;
      } catch {
      }
      const rebuilt = {
        data: body.text,
        rawData: body.raw,
        status: response.status,
        headers: contentTypeChanged ? withContentType(response.headers, body.contentType ?? "") : response.headers,
        request: response.request
      };
      if (contentTypeChanged) {
        rebuilt.mimeType = body.contentType;
      }
      return rebuilt;
    }
  };
  function setContentType(headers, contentType) {
    for (const key of Object.keys(headers ?? {})) {
      if (key.toLowerCase() === "content-type") {
        delete headers[key];
      }
    }
    headers["content-type"] = contentType;
    headers["Content-Type"] = contentType;
  }
  function withContentType(headers, contentType) {
    const next = {};
    for (const [key, value] of Object.entries(headers ?? {})) {
      if (key.toLowerCase() !== "content-type") {
        next[key] = value;
      }
    }
    next["content-type"] = contentType;
    next["Content-Type"] = contentType;
    return next;
  }
  function cookiesToRecord(cookies) {
    const record = {};
    for (const cookie of cookies ?? []) {
      record[cookie.name] = cookie.value;
    }
    return record;
  }
  function recordToCookies(record, domain) {
    return Object.entries(record).map(
      ([name, value]) => App.createCookie({ name, value, domain, path: "/" })
    );
  }

  // node_modules/html-entities/dist/esm/named-references.js
  var __assign = function() {
    __assign = Object.assign || function(t) {
      for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
      }
      return t;
    };
    return __assign.apply(this, arguments);
  };
  var pairDivider = "~";
  var blockDivider = "~~";
  function generateNamedReferences(input, prev) {
    var entities = {};
    var characters = {};
    var blocks = input.split(blockDivider);
    var isOptionalBlock = false;
    for (var i = 0; blocks.length > i; i++) {
      var entries = blocks[i].split(pairDivider);
      for (var j = 0; j < entries.length; j += 2) {
        var entity = entries[j];
        var character = entries[j + 1];
        var fullEntity = "&" + entity + ";";
        entities[fullEntity] = character;
        if (isOptionalBlock) {
          entities["&" + entity] = character;
        }
        characters[character] = fullEntity;
      }
      isOptionalBlock = true;
    }
    return prev ? { entities: __assign(__assign({}, entities), prev.entities), characters: __assign(__assign({}, characters), prev.characters) } : { entities, characters };
  }
  var bodyRegExps = {
    xml: /&(?:#\d+|#[xX][\da-fA-F]+|[0-9a-zA-Z]+);?/g,
    html4: /&notin;|&(?:nbsp|iexcl|cent|pound|curren|yen|brvbar|sect|uml|copy|ordf|laquo|not|shy|reg|macr|deg|plusmn|sup2|sup3|acute|micro|para|middot|cedil|sup1|ordm|raquo|frac14|frac12|frac34|iquest|Agrave|Aacute|Acirc|Atilde|Auml|Aring|AElig|Ccedil|Egrave|Eacute|Ecirc|Euml|Igrave|Iacute|Icirc|Iuml|ETH|Ntilde|Ograve|Oacute|Ocirc|Otilde|Ouml|times|Oslash|Ugrave|Uacute|Ucirc|Uuml|Yacute|THORN|szlig|agrave|aacute|acirc|atilde|auml|aring|aelig|ccedil|egrave|eacute|ecirc|euml|igrave|iacute|icirc|iuml|eth|ntilde|ograve|oacute|ocirc|otilde|ouml|divide|oslash|ugrave|uacute|ucirc|uuml|yacute|thorn|yuml|quot|amp|lt|gt|#\d+|#[xX][\da-fA-F]+|[0-9a-zA-Z]+);?/g,
    html5: /&centerdot;|&copysr;|&divideontimes;|&gtcc;|&gtcir;|&gtdot;|&gtlPar;|&gtquest;|&gtrapprox;|&gtrarr;|&gtrdot;|&gtreqless;|&gtreqqless;|&gtrless;|&gtrsim;|&ltcc;|&ltcir;|&ltdot;|&lthree;|&ltimes;|&ltlarr;|&ltquest;|&ltrPar;|&ltri;|&ltrie;|&ltrif;|&notin;|&notinE;|&notindot;|&notinva;|&notinvb;|&notinvc;|&notni;|&notniva;|&notnivb;|&notnivc;|&parallel;|&timesb;|&timesbar;|&timesd;|&(?:AElig
27FA~xharr~\u27F7~xlArr~\u27F8~xlarr~\u27F5~xmap~\u27FC~xnis~\u22FB~xodot~\u2A00~xopf~\u{1D569}~xoplus~\u2A01~xotime~\u2A02~xrArr~\u27F9~xrarr~\u27F6~xscr~\u{1D4CD}~xsqcup~\u2A06~xuplus~\u2A04~xutri~\u25B3~xvee~\u22C1~xwedge~\u22C0~yacy~\u044F~ycirc~\u0177~ycy~\u044B~yfr~\u{1D536}~yicy~\u0457~yopf~\u{1D56A}~yscr~\u{1D4CE}~yucy~\u044E~zacute~\u017A~zcaron~\u017E~zcy~\u0437~zdot~\u017C~zeetrf~\u2128~zfr~\u{1D537}~zhcy~\u0436~zigrarr~\u21DD~zopf~\u{1D56B}~zscr~\u{1D4CF}~~AMP~&~COPY~\xA9~GT~>~LT~<~QUOT~"~REG~\xAE', namedReferences["html4"]);

  // node_modules/html-entities/dist/esm/numeric-unicode-map.js
  var numericUnicodeMap = {
    0: 65533,
    128: 8364,
    130: 8218,
    131: 402,
    132: 8222,
    133: 8230,
    134: 8224,
    135: 8225,
    136: 710,
    137: 8240,
    138: 352,
    139: 8249,
    140: 338,
    142: 381,
    145: 8216,
    146: 8217,
    147: 8220,
    148: 8221,
    149: 8226,
    150: 8211,
    151: 8212,
    152: 732,
    153: 8482,
    154: 353,
    155: 8250,
    156: 339,
    158: 382,
    159: 376
  };

  // node_modules/html-entities/dist/esm/surrogate-pairs.js
  var fromCodePoint = String.fromCodePoint || function(astralCodePoint) {
    return String.fromCharCode(Math.floor((astralCodePoint - 65536) / 1024) + 55296, (astralCodePoint - 65536) % 1024 + 56320);
  };
  var getCodePoint = String.prototype.codePointAt ? function(input, position) {
    return input.codePointAt(position);
  } : function(input, position) {
    return (input.charCodeAt(position) - 55296) * 1024 + input.charCodeAt(position + 1) - 56320 + 65536;
  };

  // node_modules/html-entities/dist/esm/index.js
  var __assign2 = function() {
    __assign2 = Object.assign || function(t) {
      for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
          t[p] = s[p];
      }
      return t;
    };
    return __assign2.apply(this, arguments);
  };
  var allNamedReferences = __assign2(__assign2({}, namedReferences), { all: namedReferences.html5 });
  var defaultDecodeOptions = {
    scope: "body",
    level: "all"
  };
  var strict = /&(?:#\d+|#[xX][\da-fA-F]+|[0-9a-zA-Z]+);/g;
  var attribute = /&(?:#\d+|#[xX][\da-fA-F]+|[0-9a-zA-Z]+)[;=]?/g;
  var baseDecodeRegExps = {
    xml: {
      strict,
      attribute,
      body: bodyRegExps.xml
    },
    html4: {
      strict,
      attribute,
      body: bodyRegExps.html4
    },
    html5: {
      strict,
      attribute,
      body: bodyRegExps.html5
    }
  };
  var decodeRegExps = __assign2(__assign2({}, baseDecodeRegExps), { all: baseDecodeRegExps.html5 });
  var fromCharCode = String.fromCharCode;
  var outOfBoundsChar = fromCharCode(65533);
  function getDecodedEntity(entity, references, isAttribute, isStrict) {
    var decodeResult = entity;
    var decodeEntityLastChar = entity[entity.length - 1];
    if (isAttribute && decodeEntityLastChar === "=") {
      decodeResult = entity;
    } else if (isStrict && decodeEntityLastChar !== ";") {
      decodeResult = entity;
    } else {
      var decodeResultByReference = references[entity];
      if (decodeResultByReference) {
        decodeResult = decodeResultByReference;
      } else if (entity[0] === "&" && entity[1] === "#") {
        var decodeSecondChar = entity[2];
        var decodeCode = decodeSecondChar == "x" || decodeSecondChar == "X" ? parseInt(entity.substr(3), 16) : parseInt(entity.substr(2));
        decodeResult = decodeCode >= 1114111 ? outOfBoundsChar : decodeCode > 65535 ? fromCodePoint(decodeCode) : fromCharCode(numericUnicodeMap[decodeCode] || decodeCode);
      }
    }
    return decodeResult;
  }
  function decode(text, _a) {
    var _b = _a === void 0 ? defaultDecodeOptions : _a, _c = _b.level, level = _c === void 0 ? "all" : _c, _d = _b.scope, scope = _d === void 0 ? level === "xml" ? "strict" : "body" : _d;
    if (!text) {
      return "";
    }
    var decodeRegExp = decodeRegExps[level][scope];
    var references = allNamedReferences[level].entities;
    var isAttribute = scope === "attribute";
    var isStrict = scope === "strict";
    return text.replace(decodeRegExp, function(entity) {
      return getDecodedEntity(entity, references, isAttribute, isStrict);
    });
  }

  // common/Runtime.ts
  var DEFAULT_RETRIES = 1;
  var RESPONSE_CACHE_MS = 5e3;
  var REPLACEMENT = "\uFFFD";
  var BASE64_ALPHABET = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
  function utf8Decode(bytes) {
    let result = "";
    for (let index = 0; index < bytes.length; ) {
      const byte = bytes[index] ?? 0;
      let codePoint;
      let length;
      if (byte < 128) {
        codePoint = byte;
        length = 1;
      } else if ((byte & 224) === 192) {
        codePoint = byte & 31;
        length = 2;
      } else if ((byte & 240) === 224) {
        codePoint = byte & 15;
        length = 3;
      } else if ((byte & 248) === 240) {
        codePoint = byte & 7;
        length = 4;
      } else {
        result += REPLACEMENT;
        index += 1;
        continue;
      }
      if (index + length > bytes.length) {
        result += REPLACEMENT;
        break;
      }
      let malformed = false;
      for (let offset = 1; offset < length; offset += 1) {
        const continuation = bytes[index + offset] ?? 0;
        if ((continuation & 192) !== 128) {
          malformed = true;
          break;
        }
        codePoint = codePoint << 6 | continuation & 63;
      }
      if (malformed || codePoint > 1114111) {
        result += REPLACEMENT;
        index += 1;
        continue;
      }
      result += String.fromCodePoint(codePoint);
      index += length;
    }
    return result;
  }
  var ApplicationRuntime = class {
    constructor() {
      /**
       * Responses fetched a moment ago, so the same page is not asked for twice.
       *
       * 0.8 asks for a series and then for its chapters as two separate calls
       * that each identify the series by id alone, and on most sites both
       * answers live on the same page. Without this, opening a series costs two
       * identical requests. Entries are held only for a few seconds — long
       * enough to cover one screen opening, short enough that a reader who pulls
       * to refresh gets a fresh page.
       */
      this.inFlight = /* @__PURE__ */ new Map();
    }
    /**
     * Called once by the source's base class.
     *
     * The app hands each source its own cheerio instance at construction
     * rather than exposing a global one, so it is captured here and reused by
     * every parser in the source.
     */
    bind(requestManager, cheerio) {
      this.requestManager = requestManager;
      this.cheerio = cheerio;
    }
    manager() {
      if (this.requestManager === void 0) {
        throw new Error("No request manager is bound; the source was used before it finished initialising.");
      }
      return this.requestManager;
    }
    /**
     * Performs a request and returns the response alongside its body.
     *
     * The tuple shape keeps the response metadata and the decoded body
     * separate, which is what the parsers expect.
     */
    async scheduleRequest(init, retries = DEFAULT_RETRIES) {
      const key = cacheKey(init);
      if (key === void 0) {
        return this.perform(init, retries);
      }
      const now = Date.now();
      const cached = this.inFlight.get(key);
      if (cached !== void 0 && now - cached.at < RESPONSE_CACHE_MS) {
        return cached.response;
      }
      const response = this.perform(init, retries);
      this.inFlight.set(key, { at: now, response });
      void response.then(
        ([settled]) => {
          if (settled.status < 200 || settled.status >= 300) {
            this.inFlight.delete(key);
          }
        },
        () => this.inFlight.delete(key)
      );
      this.prune(now);
      return response;
    }
    async perform(init, retries) {
      const response = await this.manager().schedule(this.createRequest(init), retries);
      return [response, { text: response.data ?? "", raw: response.rawData }];
    }
    /** Drops entries that have aged out, so the map cannot grow without bound. */
    prune(now) {
      for (const [key, entry] of this.inFlight) {
        if (now - entry.at >= RESPONSE_CACHE_MS) {
          this.inFlight.delete(key);
        }
      }
    }
    /** Performs a request and returns the body as text, checking the status first. */
    async fetchText(init, retries = DEFAULT_RETRIES) {
      const [response, body] = await this.scheduleRequest(init, retries);
      assertOk(response, init.url);
      return body.text;
    }
    /** Parses an HTML string into a queryable document. */
    loadDocument(html) {
      if (this.cheerio === void 0) {
        throw new Error("No HTML parser is bound; the source was used before it finished initialising.");
      }
      return this.cheerio.load(html);
    }
    /** Performs a request and parses the response as an HTML document. */
    async fetchDocument(init, retries = DEFAULT_RETRIES) {
      return this.loadDocument(await this.fetchText(init, retries));
    }
    /**
     * Turns `&amp;` and friends back into the characters they stand for.
     *
     * Titles and descriptions routinely arrive double-encoded, so this is
     * applied to any text taken from an attribute rather than a text node.
     */
    decodeHTMLEntities(value) {
      return decode(value);
    }
    /**
     * Decodes standard or URL-safe base64 into a string.
     *
     * The runtime has no `atob`, and several sites hide their page manifests
     * behind a base64 payload, so sources get their own decoder. Padding is
     * optional and whitespace is ignored, which is how these payloads tend to
     * arrive.
     */
    base64Decode(value) {
      return utf8Decode(this.base64Bytes(value));
    }
    /**
     * Decodes base64 into the bytes themselves.
     *
     * A payload that is not text — a ciphertext, an image — has to survive as
     * bytes, and reading it as a string would replace everything that is not
     * valid UTF-8 before the source ever saw it.
     */
    base64DecodeBytes(value) {
      return Uint8Array.from(this.base64Bytes(value));
    }
    /**
     * Turns UTF-8 bytes into a string.
     *
     * The runtime has no `TextDecoder`, and a source that has just decrypted
     * something holds bytes rather than text.
     */
    utf8Decode(bytes) {
      return utf8Decode(Array.from(bytes));
    }
    base64Bytes(value) {
      const clean = value.replace(/[\r\n\s]/g, "").replace(/-/g, "+").replace(/_/g, "/");
      let bits = 0;
      let accumulator = 0;
      const bytes = [];
      for (const character of clean) {
        if (character === "=") {
          break;
        }
        const index = BASE64_ALPHABET.indexOf(character);
        if (index === -1) {
          throw new Error("The value is not valid base64.");
        }
        accumulator = accumulator << 6 | index;
        bits += 6;
        if (bits >= 8) {
          bits -= 8;
          bytes.push(accumulator >> bits & 255);
        }
      }
      return bytes;
    }
    /** Performs a request and parses the body as JSON. */
    async fetchJSON(init, retries = DEFAULT_RETRIES) {
      const text = await this.fetchText(init, retries);
      try {
        return JSON.parse(text);
      } catch {
        throw new Error(`Expected JSON but the response could not be parsed: ${init.url}`);
      }
    }
    /** Builds a runtime request from a source's description of one. */
    createRequest(init) {
      return App.createRequest({
        url: init.url,
        method: init.method ?? "GET",
        headers: init.headers ?? {},
        param: init.param,
        data: encodeBody(init.body),
        // A cookie set on the request itself is how a source asks for the
        // ungated answer — an age gate, a language, a session. Dropping it
        // here would send the request bare and quietly return the wrong page.
        cookies: Object.entries(init.cookies ?? {}).map(
          ([name, value]) => App.createCookie({ name, value, domain: hostOf(init.url), path: "/" })
        )
      });
    }
    /** Returns the body text of a response. */
    arrayBufferToUTF8String(body) {
      return body.text;
    }
    /**
     * Asks the app to rebuild the home page.
     *
     * 0.8 has no way to invalidate it — the app decides when to rebuild — so
     * this does nothing. Sources call it after a setting changes, and the new
     * sections appear the next time the reader opens the page.
     */
    invalidateDiscoverSections() {
    }
    /** Waits for a number of seconds. */
    async sleep(seconds) {
      await new Promise((resolve) => setTimeout(resolve, Math.max(0, seconds) * 1e3));
    }
    /**
     * The user agent the app uses for its own requests.
     *
     * Matching it matters on sites that fingerprint clients, and it is cached
     * because the lookup crosses into the host app.
     */
    async getDefaultUserAgent() {
      this.userAgent ??= await this.manager().getDefaultUserAgent();
      return this.userAgent;
    }
  };
  function assertOk(response, url) {
    if (response.status === 404) {
      throw new Error(`Not found: ${url}`);
    }
    if (response.status >= 200 && response.status < 300) {
      return;
    }
    if (isCloudflareChallenge(response.status, response.headers, response.data ?? "")) {
      throw new CloudflareError(response.request);
    }
    throw new Error(`Request failed with status ${response.status}: ${url}`);
  }
  function cacheKey(init) {
    if (init.body !== void 0) {
      return void 0;
    }
    const method = (init.method ?? "GET").toUpperCase();
    if (method !== "GET") {
      return void 0;
    }
    return `${init.url} ${init.param ?? ""} ${pairs(init.headers)} ${pairs(init.cookies)}`;
  }
  function pairs(record) {
    return Object.entries(record ?? {}).map(([name, value]) => `${name.toLowerCase()}=${value}`).sort().join(";");
  }
  function encodeBody(body) {
    if (body === void 0 || typeof body === "string") {
      return body;
    }
    return Object.entries(body).filter(([, value]) => value !== void 0 && value !== null).map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(String(value))}`).join("&");
  }
  var Application = new ApplicationRuntime();

  // common/Discover.ts
  var import_types2 = __toESM(require_lib());
  function homeSectionType(type) {
    switch (type) {
      case "featured" /* featured */:
        return import_types2.HomeSectionType.featured;
      case "prominentCarousel" /* prominentCarousel */:
        return import_types2.HomeSectionType.singleRowLarge;
      case "simpleCarousel" /* simpleCarousel */:
      case "chapterUpdates" /* chapterUpdates */:
      case "genres" /* genres */:
        return import_types2.HomeSectionType.singleRowNormal;
    }
  }
  function isRenderableSection(type) {
    return type !== "genres" /* genres */;
  }

  // common/Convert.ts
  function toSourceManga(manga) {
    const info = manga.mangaInfo;
    return App.createSourceManga({
      id: manga.mangaId,
      mangaInfo: App.createMangaInfo({
        image: info.thumbnailUrl,
        titles: [info.primaryTitle, ...info.secondaryTitles].filter(
          (title, index, all) => title.length > 0 && all.indexOf(title) === index
        ),
        desc: info.synopsis,
        status: info.status,
        hentai: info.contentRating === "ADULT" /* ADULT */,
        author: info.author,
        artist: info.artist,
        banner: info.bannerUrl,
        rating: info.rating,
        tags: (info.tagGroups ?? []).map(toTagSection),
        covers: [info.thumbnailUrl].filter((url) => url.length > 0),
        additionalInfo: info.additionalInfo
      })
    });
  }
  function toTagSection(section) {
    return App.createTagSection({
      id: section.id,
      label: section.title,
      tags: section.tags.map((tag) => App.createTag({ id: tag.id, label: tag.title }))
    });
  }
  function toChapter(chapter) {
    return App.createChapter({
      id: chapter.chapterId,
      chapNum: chapter.chapNum,
      name: chapter.title,
      volume: chapter.volume,
      group: chapter.group,
      time: chapter.publishDate,
      langCode: chapter.langCode ?? "\u{1F1EC}\u{1F1E7}",
      sortingIndex: chapter.sortingIndex
    });
  }
  function toChapterDetails(details) {
    return App.createChapterDetails({
      id: details.id,
      mangaId: details.mangaId,
      pages: details.pages
    });
  }
  function toPartialFromSearch(item) {
    return App.createPartialSourceManga({
      mangaId: item.mangaId,
      title: item.title,
      image: item.imageUrl,
      subtitle: item.subtitle
    });
  }
  function toPartialFromDiscover(item) {
    return App.createPartialSourceManga({
      mangaId: item.mangaId,
      title: item.title,
      image: item.imageUrl,
      subtitle: item.subtitle ?? item.chapterId
    });
  }
  function toPagedResults(results, metadata) {
    return App.createPagedResults({
      results,
      // A section with no follow-up page must report no metadata, otherwise
      // the app keeps asking for more and repeats the last page forever.
      metadata: metadata ?? void 0
    });
  }
  function toHomeSection(section, items, containsMoreItems) {
    return App.createHomeSection({
      id: section.id,
      title: section.title,
      type: homeSectionType(section.type),
      items,
      containsMoreItems
    });
  }

  // common/Menu.ts
  function selectRow(id, options) {
    const titles = new Map(options.options.map((option) => [option.id, option.title]));
    return App.createDUISelect({
      id,
      label: options.label,
      options: options.options.map((option) => option.id),
      allowsMultiselect: options.multiple ?? false,
      // The app stores ids and asks separately for something to display.
      labelResolver: async (optionId) => titles.get(optionId) ?? optionId,
      value: App.createDUIBinding({
        get: async () => options.get(),
        set: async (value) => {
          options.set(Array.isArray(value) ? value.filter((entry) => typeof entry === "string") : []);
        }
      })
    });
  }
  function toSection(section) {
    return App.createDUISection({
      id: section.id,
      header: section.header,
      footer: section.footer,
      isHidden: false,
      rows: async () => section.rows
    });
  }
  function settingsMenu(title, sections) {
    const form = App.createDUIForm({
      // Built when the screen opens rather than up front, so a source whose
      // settings depend on something fetched can wait for it here.
      sections: async () => (await sections()).map(toSection)
    });
    return App.createDUISection({
      id: "settings",
      isHidden: false,
      rows: async () => [App.createDUINavigationButton({ id: "settings_button", label: title, form })]
    });
  }

  // common/Settings.ts
  var SettingsStore = class {
    constructor(stateManager, keys) {
      this.cache = /* @__PURE__ */ new Map();
      this.stateManager = stateManager;
      this.keys = keys;
    }
    /**
     * Reads every declared key into memory.
     *
     * Safe to call repeatedly — the first call is the one that does the work,
     * and later callers wait on it rather than starting again.
     */
    load() {
      return this.hydration ??= this.hydrate();
    }
    async hydrate() {
      await Promise.all(
        this.keys.map(async (key) => {
          try {
            const value = await this.stateManager.retrieve(key);
            if (value !== null && value !== void 0 && !this.cache.has(key)) {
              this.cache.set(key, value);
            }
          } catch {
          }
        })
      );
    }
    /** The raw stored value, or `undefined` when nothing is set. */
    get(key) {
      return this.cache.get(key);
    }
    /** Stores a value, taking effect immediately and persisting behind. */
    set(key, value) {
      this.cache.set(key, value);
      void this.persist(key, value);
    }
    async persist(key, value) {
      try {
        await this.stateManager.store(key, value);
      } catch {
      }
    }
    /** A stored string, or the fallback when unset or empty. */
    string(key, fallback) {
      const value = this.get(key);
      if (typeof value !== "string") {
        return fallback;
      }
      const trimmed = value.trim();
      return trimmed.length > 0 ? trimmed : fallback;
    }
    /** A stored list of strings, keeping only the values still recognised. */
    stringArray(key, allowed) {
      const value = this.get(key);
      if (!Array.isArray(value)) {
        return [];
      }
      return value.filter(
        (entry) => typeof entry === "string" && (allowed === void 0 || allowed.has(entry))
      );
    }
    /** A stored value constrained to a known set of choices. */
    choice(key, options, fallback) {
      const value = this.get(key);
      return options.includes(value) ? value : fallback;
    }
    boolean(key, fallback) {
      const value = this.get(key);
      return typeof value === "boolean" ? value : fallback;
    }
  };

  // common/Source.ts
  var PopmangoSource = class {
    constructor(cheerio, options) {
      /** Chapters of the series above, so page lookups have the full chapter. */
      this.lastChapters = /* @__PURE__ */ new Map();
      this.cheerio = cheerio;
      this.domain = options.domain;
      this.stateManager = App.createSourceStateManager();
      this.cookieStorage = new CookieStorageInterceptor(this.stateManager);
      this.settings = new SettingsStore(this.stateManager, options.settingsKeys ?? []);
      this.requestManager = App.createRequestManager({
        requestsPerSecond: options.rateLimit ? Math.max(1, Math.round(options.rateLimit.numberOfRequests / Math.max(1, options.rateLimit.bufferInterval))) : 4,
        requestTimeout: (options.requestTimeout ?? 20) * 1e3,
        interceptor: new InterceptorChain({
          rateLimiter: options.rateLimit ? new BasicRateLimiter(options.rateLimit) : void 0,
          cookieStorage: this.cookieStorage,
          interceptor: options.interceptor
        })
      });
      Application.bind(this.requestManager, cheerio);
    }
    /**
     * Filters offered on the search screen.
     *
     * 0.8 renders these as selectable tags. A source with a sort order to
     * offer exposes it here as a section of mutually exclusive tags, since
     * 0.8 has no separate sort control.
     */
    async getFilterSections() {
      return [];
    }
    /**
     * Free-text boxes offered beside the filters.
     *
     * A 0.9 search form could hold anything; 0.8 renders selectable tags and
     * these boxes, and nothing else. A filter that is not a choice from a list
     * belongs here — what the reader types comes back in
     * `SearchQuery.parameters` under the field's id.
     */
    async getSearchFieldList() {
      return [];
    }
    /** Whether the site can express "everything except this tag". */
    async supportsTagExclusion() {
      return false;
    }
    /** The public URL of a series, for the share sheet. */
    getMangaShareUrl(mangaId) {
      return `${this.domain}/${mangaId}`;
    }
    /**
     * The source's settings screen, if it has one.
     *
     * Rows are built with the helpers in `Menu.ts` and read and write through
     * `this.settings`. Returning `[]` means the source has no settings, and
     * the app is told as much.
     */
    getSettingsSections() {
      return [];
    }
    // ---------------------------------------------------------------------
    // What the app calls
    // ---------------------------------------------------------------------
    async getMangaDetails(mangaId) {
      const manga = await this.guard(() => this.getMangaInfo(mangaId));
      this.lastManga = manga;
      return toSourceManga(manga);
    }
    /**
     * 0.8 asks for chapters by id alone, while a source is given the whole
     * series so its parsers can reuse what the details page already said. The
     * details are taken from the previous call when they are for this series —
     * which is the normal case, since the app opens the details screen first —
     * and fetched otherwise.
     */
    async getChapters(mangaId) {
      return this.guard(async () => {
        const manga = this.lastManga?.mangaId === mangaId ? this.lastManga : await this.getMangaInfo(mangaId);
        this.lastManga = manga;
        const chapters = await this.getChapterList(manga);
        this.lastChapters = new Map(chapters.map((chapter) => [chapter.chapterId, chapter]));
        return chapters.map(toChapter);
      });
    }
    /**
     * 0.8 asks for pages by ids alone. The chapter object is recovered from
     * the list fetched a moment ago; if the app skipped straight here — as it
     * does when opening a chapter from the reading queue after a restart — the
     * list is fetched again so the source still receives a complete chapter.
     */
    async getChapterDetails(mangaId, chapterId) {
      return this.guard(async () => {
        let chapter = this.lastChapters.get(chapterId);
        if (chapter === void 0 || chapter.sourceManga.mangaId !== mangaId) {
          await this.getChapters(mangaId);
          chapter = this.lastChapters.get(chapterId);
        }
        if (chapter === void 0) {
          throw new Error(`Chapter ${chapterId} is no longer listed under ${mangaId}.`);
        }
        return toChapterDetails(await this.getPages(chapter));
      });
    }
    async getSearchResults(query, metadata) {
      return this.guard(async () => {
        const page = await this.getSearchResultItems(
          {
            title: query.title?.trim() === "" ? void 0 : query.title,
            includedTags: query.includedTags.map((tag) => ({ id: tag.id, title: tag.label })),
            excludedTags: query.excludedTags.map((tag) => ({ id: tag.id, title: tag.label })),
            parameters: query.parameters ?? {}
          },
          metadata
        );
        return toPagedResults(page.items.map(toPartialFromSearch), page.metadata);
      });
    }
    async getSourceMenu() {
      await this.settings.load();
      return settingsMenu("Settings", () => this.getSettingsSections());
    }
    async getSearchTags() {
      const sections = await this.guard(() => this.getFilterSections());
      return sections.map(toTagSection);
    }
    async getSearchFields() {
      const fields = await this.guard(() => this.getSearchFieldList());
      return fields.map(
        (field) => App.createSearchField({ id: field.id, name: field.name, placeholder: field.placeholder })
      );
    }
    /**
     * Builds the home page.
     *
     * Each section is announced empty first so the app can lay the page out
     * immediately, then filled in as its request comes back. Sections are
     * fetched together rather than in turn, and one that fails is left empty
     * instead of taking the whole page down with it.
     *
     * The app tracks a section by object identity rather than by id, so the
     * placeholder is kept and its items are set on it — announcing a second,
     * equal-looking section instead leaves the page with duplicates or with
     * rows that never fill in.
     */
    async getHomePageSections(sectionCallback) {
      const sections = (await this.guard(() => this.getDiscoverSections())).filter(
        (section) => isRenderableSection(section.type)
      );
      this.sections = sections;
      const placeholders = sections.map((section) => toHomeSection(section, [], false));
      for (const placeholder of placeholders) {
        sectionCallback(placeholder);
      }
      await Promise.all(
        sections.map(async (section, index) => {
          const placeholder = placeholders[index];
          if (placeholder === void 0) {
            return;
          }
          let items = [];
          let hasMore = false;
          try {
            const page = await this.getDiscoverSectionItems(section, void 0);
            items = page.items.map(toPartialFromDiscover);
            hasMore = page.metadata !== void 0;
          } catch (error) {
            this.rememberChallenge(error);
          }
          placeholder.items = items;
          placeholder.containsMoreItems = hasMore;
          sectionCallback(placeholder);
        })
      );
    }
    async getViewMoreItems(homepageSectionId, metadata) {
      return this.guard(async () => {
        if (this.sections === void 0 || this.sections.length === 0) {
          this.sections = await this.getDiscoverSections();
        }
        const section = this.sections.find((candidate) => candidate.id === homepageSectionId);
        if (section === void 0) {
          throw new Error(`Unknown home page section: ${homepageSectionId}`);
        }
        const page = await this.getDiscoverSectionItems(section, metadata);
        return toPagedResults(page.items.map(toPartialFromDiscover), page.metadata);
      });
    }
    /**
     * Points the app at a page that will settle the challenge.
     *
     * The URL that actually failed is preferred, because a challenge is often
     * scoped to a path rather than the whole site.
     */
    async getCloudflareBypassRequestAsync() {
      if (this.pendingChallenge !== void 0) {
        return this.pendingChallenge;
      }
      return App.createRequest({
        url: this.domain,
        method: "GET",
        headers: { "user-agent": await Application.getDefaultUserAgent() }
      });
    }
    // ---------------------------------------------------------------------
    // Internals
    // ---------------------------------------------------------------------
    /**
     * Runs a source call, noting any Cloudflare challenge on the way past.
     *
     * The error is still rethrown — the app shows the bypass prompt in
     * response to it — but by then the failing request has been recorded so
     * the prompt opens the right page.
     */
    async guard(work) {
      try {
        await this.settings.load();
        return await work();
      } catch (error) {
        this.rememberChallenge(error);
        throw error;
      }
    }
    rememberChallenge(error) {
      if (error instanceof CloudflareError) {
        this.pendingChallenge = error.request;
      }
    }
  };

  // src/XCOMIC/models.ts
  var DOMAIN = "https://xcomic.me";
  var API_URL = `${DOMAIN}/query/`;
  var PAGE_SIZE = 36;
  var CHAPTER_PAGE_SIZE = 1e3;
  var RECENTLY_ADDED_SIZE = 50;
  var MAX_LATEST_REQUESTS = 10;
  var BROWSE_QUERY = `
query get_comic_browse_items($select: Comic_Browse_Select) {
  get_comic_browse_items(select: $select) {
    data {
      id name
      urlCover
      translatedLanguage
      type contentRating genres tags
      summary { html }
      sfw_result score_val chaps_normal
      chapterNodes_last(amount: 1) {
        data {
          serial chaNum
        }
      }
    }
  }
}
`;
  var LATEST_UPLOADS_QUERY = `
query get_comic_latestUploads($select: Comic_LatestUploads_Select) {
  get_comic_latestUploads(select: $select) {
    before
    items {
      comic {
        data {
          id name urlPath urlCover
          translatedLanguage
          type contentRating genres tags sfw_result
        }
      }
      chapters(amount: 1) {
        data {
          id serial chaNum urlPath
          dateCreate dateModify datePublic
        }
      }
    }
  }
}
`;
  var RECENTLY_ADDED_QUERY = `
query get_comic_recentlyAdded($select: Comic_RecentlyAdded_Select) {
  get_comic_recentlyAdded(select: $select) {
    before
    items {
      data {
        id name urlPath urlCover
        translatedLanguage
        type contentRating genres tags sfw_result
      }
    }
  }
}
`;
  var COMIC_QUERY = `
query get_comicNode($id: ID!) {
  get_comicNode(id: $id) {
    data {
      id name altNames
      originalLanguage translatedLanguage
      originalStatus originalPubFrom { y m d }
      originalPubTill { y m d }
      originalPubZone uploadStatus
      type demographics contentRating genres tags
      authorNodes { data { name } }
      artistNodes { data { name } }
      tagNodes { data { name } }
      publisherNodes { data { name } }
      summary { html }
      urlPath urlCover
      sfw_result score_val follows reviews comments_total chaps_normal
    }
  }
}
`;
  var CHAPTERS_QUERY = `
query get_comic_chapterList_uniqList($select: Select_Comic_ChapterList_UniqList) {
  get_comic_chapterList_uniqList(select: $select) {
    paging { pages }
    items {
      data {
        id dbStatus serial chaNum
        dname title urlPath
        dateCreate dateModify datePublic
        srcName
        profileNodes { data { name } }
        userNode { data { name } }
        groupNodes { data { name } }
      }
    }
  }
}
`;
  var CHAPTER_PAGES_QUERY = `
query get_chapterNode($id: ID!) {
  get_chapterNode(id: $id) {
    data { imageUrls }
  }
}
`;
  var SECTIONS = {
    TOP_RATED: "top-rated",
    LATEST_UPLOADS: "latest-uploads",
    RECENTLY_ADDED: "recently-added"
  };
  var DISCOVER_SECTIONS = {
    [SECTIONS.TOP_RATED]: {
      id: SECTIONS.TOP_RATED,
      title: "Top Rated",
      type: "featured" /* featured */
    },
    [SECTIONS.LATEST_UPLOADS]: {
      id: SECTIONS.LATEST_UPLOADS,
      title: "Latest Uploads",
      type: "chapterUpdates" /* chapterUpdates */
    },
    [SECTIONS.RECENTLY_ADDED]: {
      id: SECTIONS.RECENTLY_ADDED,
      title: "Recently Added",
      type: "simpleCarousel" /* simpleCarousel */
    }
  };
  var SECTION_IDS = Object.values(SECTIONS);
  var SECTION_OPTIONS = SECTION_IDS.map((id) => ({
    id,
    title: DISCOVER_SECTIONS[id].title
  }));
  var STATE_KEYS = {
    CONTENT_RATINGS: "xcomic_content_ratings",
    CONTENT_TYPES: "xcomic_content_types",
    EXCLUDED_GENRES: "xcomic_excluded_genres",
    EXCLUDED_FORMATS: "xcomic_excluded_formats",
    LANGUAGES: "xcomic_languages",
    VISIBLE_SECTIONS: "xcomic_visible_sections"
  };
  var SETTINGS_KEYS = Object.values(STATE_KEYS);
  var CONTENT_RATING_GENRES = {
    suggestive: ["ecchi", "mature", "yaoi", "yuri"],
    erotica: ["adult", "erotica", "smut"],
    pornographic: ["hentai", "pornographic"]
  };
  var DEFAULT_CONTENT_RATINGS = [
    "safe",
    "suggestive",
    "erotica",
    "pornographic"
  ];
  var DEFAULT_CONTENT_TYPES = [
    "artbook",
    "cartoon",
    "imageset",
    "manga",
    "manhua",
    "manhwa",
    "western"
  ];
  var DEFAULT_LANGUAGES = ["en"];
  var CONTENT_RATING_OPTIONS = [
    { id: "safe", title: "Safe" },
    { id: "suggestive", title: "Suggestive" },
    { id: "erotica", title: "Erotica" },
    { id: "pornographic", title: "Pornographic" }
  ];
  var CONTENT_TYPE_OPTIONS = [
    { id: "manga", title: "Manga" },
    { id: "manhwa", title: "Manhwa" },
    { id: "manhua", title: "Manhua" },
    { id: "western", title: "Western" },
    { id: "cartoon", title: "Cartoon" },
    { id: "artbook", title: "Artbook" },
    { id: "imageset", title: "Image Set" }
  ];
  var TRANSLATED_LANGUAGE_KEY = "Translated Language";
  var TAG_TITLE_OVERRIDES = {
    silver_golden: "Silver & Golden",
    non_human: "Non-human"
  };
  var STATUS_OPTIONS = [
    { id: "pending", title: "Pending" },
    { id: "ongoing", title: "Ongoing" },
    { id: "completed", title: "Completed" },
    { id: "hiatus", title: "Hiatus" },
    { id: "cancelled", title: "Cancelled" }
  ];
  var MODE_OPTIONS = [
    { id: "and", title: "Match all" },
    { id: "or", title: "Match any" }
  ];
  var CHAPTER_COUNT_OPTIONS = [
    { id: "0", title: "0" },
    { id: "1", title: "1+" },
    { id: "10", title: "10+" },
    { id: "20", title: "20+" },
    { id: "30", title: "30+" },
    { id: "40", title: "40+" },
    { id: "50", title: "50+" },
    { id: "60", title: "60+" },
    { id: "70", title: "70+" },
    { id: "80", title: "80+" },
    { id: "90", title: "90+" },
    { id: "100", title: "100+" },
    { id: "200", title: "200+" },
    { id: "300", title: "300+" },
    { id: "1-9", title: "1\u20139" },
    { id: "10-19", title: "10\u201319" },
    { id: "20-29", title: "20\u201329" },
    { id: "30-39", title: "30\u201339" },
    { id: "40-49", title: "40\u201349" },
    { id: "50-59", title: "50\u201359" },
    { id: "60-69", title: "60\u201369" },
    { id: "70-79", title: "70\u201379" },
    { id: "80-89", title: "80\u201389" },
    { id: "90-99", title: "90\u201399" },
    { id: "100-199", title: "100\u2013199" },
    { id: "200-299", title: "200\u2013299" }
  ];
  var FORMAT_OPTIONS = [
    { id: "4_koma", title: "4 Koma" },
    { id: "adaptation", title: "Adaptation" },
    { id: "anthology", title: "Anthology" },
    { id: "award_winning", title: "Award Winning" },
    { id: "doujinshi", title: "Doujinshi" },
    { id: "fan_colored", title: "Fan Colored" },
    { id: "full_color", title: "Full Color" },
    { id: "long_strip", title: "Long Strip" },
    { id: "official_colored", title: "Official Colored" },
    { id: "oneshot", title: "Oneshot" },
    { id: "web_comic", title: "Web Comic" },
    { id: "webtoon", title: "Webtoon" }
  ];
  var LANGUAGE_OPTIONS = [
    { id: "en", title: "English" },
    { id: "ab", title: "Abkhazian" },
    { id: "af", title: "Afrikaans" },
    { id: "sq", title: "Albanian" },
    { id: "ar", title: "Arabic" },
    { id: "hy", title: "Armenian" },
    { id: "az", title: "Azerbaijani" },
    { id: "eu", title: "Basque" },
    { id: "be", title: "Belarusian" },
    { id: "bn", title: "Bengali" },
    { id: "bs", title: "Bosnian" },
    { id: "bg", title: "Bulgarian" },
    { id: "my", title: "Burmese" },
    { id: "km", title: "Cambodian" },
    { id: "ca", title: "Catalan" },
    { id: "ceb", title: "Cebuano" },
    { id: "zh", title: "Chinese" },
    { id: "zh_hk", title: "Chinese (Cantonese)" },
    { id: "zh_tw", title: "Chinese (Traditional)" },
    { id: "cv", title: "Chuvash" },
    { id: "hr", title: "Croatian" },
    { id: "cs", title: "Czech" },
    { id: "da", title: "Danish" },
    { id: "nl", title: "Dutch" },
    { id: "eo", title: "Esperanto" },
    { id: "et", title: "Estonian" },
    { id: "fil", title: "Filipino" },
    { id: "fi", title: "Finnish" },
    { id: "fr", title: "French" },
    { id: "gl", title: "Galician" },
    { id: "ka", title: "Georgian" },
    { id: "de", title: "German" },
    { id: "el", title: "Greek" },
    { id: "gn", title: "Guarani" },
    { id: "gu", title: "Gujarati" },
    { id: "ht", title: "Haitian Creole" },
    { id: "he", title: "Hebrew" },
    { id: "hi", title: "Hindi" },
    { id: "hu", title: "Hungarian" },
    { id: "is", title: "Icelandic" },
    { id: "ig", title: "Igbo" },
    { id: "id", title: "Indonesian" },
    { id: "ga", title: "Irish" },
    { id: "it", title: "Italian" },
    { id: "ja", title: "Japanese" },
    { id: "jv", title: "Javanese" },
    { id: "kk", title: "Kazakh" },
    { id: "ko", title: "Korean" },
    { id: "ku", title: "Kurdish" },
    { id: "ky", title: "Kyrgyz" },
    { id: "lo", title: "Laothian" },
    { id: "la", title: "Latin" },
    { id: "lv", title: "Latvian" },
    { id: "lt", title: "Lithuanian" },
    { id: "mg", title: "Malagasy" },
    { id: "ms", title: "Malay" },
    { id: "ml", title: "Malayalam" },
    { id: "mt", title: "Maltese" },
    { id: "mi", title: "Maori" },
    { id: "mr", title: "Marathi" },
    { id: "mo", title: "Moldavian" },
    { id: "mn", title: "Mongolian" },
    { id: "ne", title: "Nepali" },
    { id: "no", title: "Norwegian" },
    { id: "ny", title: "Nyanja" },
    { id: "ps", title: "Pashto" },
    { id: "fa", title: "Persian" },
    { id: "pl", title: "Polish" },
    { id: "pt", title: "Portuguese" },
    { id: "pt_br", title: "Portuguese (Brazil)" },
    { id: "ro", title: "Romanian" },
    { id: "ru", title: "Russian" },
    { id: "sr", title: "Serbian" },
    { id: "sh", title: "Serbo-Croatian" },
    { id: "st", title: "Sesotho" },
    { id: "si", title: "Sinhalese" },
    { id: "sk", title: "Slovak" },
    { id: "sl", title: "Slovenian" },
    { id: "so", title: "Somali" },
    { id: "es", title: "Spanish" },
    { id: "es_419", title: "Spanish (Latin America)" },
    { id: "ss", title: "Swati" },
    { id: "sv", title: "Swedish" },
    { id: "ta", title: "Tamil" },
    { id: "te", title: "Telugu" },
    { id: "th", title: "Thai" },
    { id: "ti", title: "Tigrinya" },
    { id: "to", title: "Tonga" },
    { id: "tr", title: "Turkish" },
    { id: "tk", title: "Turkmen" },
    { id: "uk", title: "Ukrainian" },
    { id: "ur", title: "Urdu" },
    { id: "uz", title: "Uzbek" },
    { id: "vi", title: "Vietnamese" },
    { id: "yo", title: "Yoruba" },
    { id: "zu", title: "Zulu" },
    { id: "_t", title: "Other" }
  ];
  var MOST_VIEWS_OPTIONS = [
    { id: "views_d000", title: "Most viewed (all time)" },
    { id: "views_d360", title: "Most viewed (360 days)" },
    { id: "views_d180", title: "Most viewed (180 days)" },
    { id: "views_d090", title: "Most viewed (90 days)" },
    { id: "views_d030", title: "Most viewed (30 days)" },
    { id: "views_d007", title: "Most viewed (7 days)" },
    { id: "views_h024", title: "Most viewed (24 hours)" },
    { id: "views_h012", title: "Most viewed (12 hours)" },
    { id: "views_h006", title: "Most viewed (6 hours)" },
    { id: "views_h001", title: "Most viewed (1 hour)" }
  ];
  var SORT_OPTIONS = [
    { id: "field_score", title: "Rating Score" },
    { id: "field_update", title: "Latest Update" },
    { id: "field_create", title: "Recently Added" },
    { id: "field_name_asc", title: "Name A-Z" },
    { id: "field_name_desc", title: "Name Z-A" },
    { id: "field_chapter", title: "Most Chapters" },
    { id: "field_follow", title: "Most Follows" },
    { id: "field_review", title: "Most Reviews" },
    { id: "field_comment", title: "Most Comments" }
  ];
  var FILTERS = {
    SORT: "sort",
    VIEWS: "views",
    GENRE: "genre",
    FORMAT: "format",
    DEMOGRAPHIC: "demographic",
    ORIG_STATUS: "origstatus",
    SITE_STATUS: "sitestatus",
    CHAPTERS: "chapters",
    TLANG: "tlang",
    OLANG: "olang",
    INC_MODE: "incmode",
    EXC_MODE: "excmode"
  };
  var FILTER_SEPARATOR = "::";
  function filterTag(section, id, title) {
    return { id: `${section}${FILTER_SEPARATOR}${id}`, title };
  }
  function splitFilterTag(tagId) {
    const index = tagId.indexOf(FILTER_SEPARATOR);
    if (index <= 0) {
      return void 0;
    }
    return { section: tagId.slice(0, index), value: tagId.slice(index + FILTER_SEPARATOR.length) };
  }
  var YEAR_FIELD = "year";

  // src/XCOMIC/network.ts
  var XComicInterceptor = class extends PaperbackInterceptor {
    async interceptRequest(request) {
      return {
        ...request,
        headers: {
          ...request.headers,
          referer: `${DOMAIN}/`,
          origin: DOMAIN,
          "user-agent": await Application.getDefaultUserAgent()
        }
      };
    }
    async interceptResponse(request, response, body) {
      const contentType = headerValue(response.headers, "content-type") ?? "";
      const text = contentType.includes("text/html") ? body.text : "";
      if (isCloudflareChallenge(response.status, response.headers, text)) {
        throw new CloudflareError(
          Application.createRequest({
            // The data endpoint cannot render the challenge, so it is
            // sent to the site root where the reader can clear it.
            url: request.url.startsWith(API_URL) ? `${DOMAIN}/` : request.url,
            method: "GET",
            headers: { "user-agent": await Application.getDefaultUserAgent() }
          })
        );
      }
      return body;
    }
  };
  async function fetchGraphQL(query, variables) {
    const body = await Application.fetchText({
      url: API_URL,
      method: "POST",
      headers: { accept: "application/json", "content-type": "application/json" },
      body: JSON.stringify({ query: query.trim(), variables })
    });
    let parsed;
    try {
      parsed = JSON.parse(body);
    } catch {
      throw new Error("The site's answer could not be read.");
    }
    if (parsed.errors !== null && parsed.errors !== void 0 && parsed.errors.length > 0) {
      throw new Error(parsed.errors.map((error) => error.message ?? "Unknown error").join("\n"));
    }
    if (parsed.data === null || parsed.data === void 0) {
      throw new Error("The site returned an empty answer.");
    }
    return parsed.data;
  }
  function fetchBrowse(select) {
    return fetchGraphQL(BROWSE_QUERY, { select });
  }
  function fetchLatestUploads(before) {
    return fetchGraphQL(LATEST_UPLOADS_QUERY, {
      select: { size: PAGE_SIZE, ...before !== void 0 ? { before } : {} }
    });
  }
  function fetchRecentlyAdded() {
    return fetchGraphQL(RECENTLY_ADDED_QUERY, {
      select: { size: RECENTLY_ADDED_SIZE }
    });
  }
  function fetchComic(id) {
    return fetchGraphQL(COMIC_QUERY, { id });
  }
  function fetchChapters(comicId, page) {
    return fetchGraphQL(CHAPTERS_QUERY, {
      select: { comic_id: comicId, page, size: CHAPTER_PAGE_SIZE, sortby: "chapter_desc" }
    });
  }
  function fetchChapterPages(id) {
    return fetchGraphQL(CHAPTER_PAGES_QUERY, { id });
  }
  function fetchSearchPage() {
    return Application.fetchText({
      url: `${DOMAIN}/search`,
      method: "GET",
      headers: { accept: "text/html,application/xhtml+xml" }
    });
  }

  // src/XCOMIC/parsers.ts
  var PORNOGRAPHIC_GENRES = new Set(CONTENT_RATING_GENRES.pornographic);
  var EROTICA_GENRES = new Set(CONTENT_RATING_GENRES.erotica);
  var SUGGESTIVE_GENRES = new Set(CONTENT_RATING_GENRES.suggestive);
  var UNSAFE_ID = /[^a-zA-Z0-9._\-@()[\]%?#+=/&:]/g;
  function sanitizeId(value) {
    return value.replace(UNSAFE_ID, "-");
  }
  function toAbsoluteUrl(url) {
    if (typeof url !== "string" || url.trim().length === 0) {
      return "";
    }
    const normalised = url.trim();
    if (/^https?:\/\//i.test(normalised)) {
      return normalised;
    }
    if (normalised.startsWith("//")) {
      return `https:${normalised}`;
    }
    return `${DOMAIN}${normalised.startsWith("/") ? "" : "/"}${normalised}`;
  }
  function hasCoverUrl(url) {
    return typeof url === "string" && url.trim().length > 0;
  }
  function titleCase(value) {
    return value.replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase());
  }
  function parseFilterOptions(html) {
    const $ = Application.loadDocument(html);
    const filterGroup = (name) => {
      const seen = /* @__PURE__ */ new Set();
      return $("details.group").filter(
        (_, element) => $(element).find("summary").first().text().trim().toLowerCase().includes(name)
      ).first().find("div").map((_, element) => {
        const raw = $(element).attr(":")?.trim();
        const title = $(element).find("span").first().text().trim();
        if (raw === void 0 || raw.length === 0 || title.length === 0) {
          return void 0;
        }
        const id = sanitizeId(raw);
        if (seen.has(id)) {
          return void 0;
        }
        seen.add(id);
        return { id, title: Application.decodeHTMLEntities(title) };
      }).get().filter((option) => option !== void 0);
    };
    const formatIds = new Set(FORMAT_OPTIONS.map(({ id }) => id));
    const options = {
      contentRatings: filterGroup("content rating"),
      demographics: filterGroup("demographics"),
      genres: filterGroup("genres").filter(({ id }) => !formatIds.has(id)),
      types: filterGroup("types")
    };
    if (options.genres.length === 0 && options.types.length === 0) {
      throw new Error("The site's search filters could not be read.");
    }
    return options;
  }
  function contentPreferenceRating(comic) {
    const taxonomy = [...comic.genres ?? [], ...comic.tags ?? []].map(
      (value) => value.trim().toLowerCase()
    );
    const rating = comic.contentRating?.trim().toLowerCase();
    if (rating !== void 0 && rating.length > 0 && !CONTENT_RATING_OPTIONS.some((option) => option.id === rating)) {
      return "pornographic";
    }
    if (rating === "pornographic" || taxonomy.some((value) => PORNOGRAPHIC_GENRES.has(value))) {
      return "pornographic";
    }
    if (rating === "erotica" || taxonomy.some((value) => EROTICA_GENRES.has(value))) {
      return "erotica";
    }
    if (rating === "suggestive" || comic.sfw_result === false || taxonomy.some((value) => SUGGESTIVE_GENRES.has(value))) {
      return "suggestive";
    }
    return "safe";
  }
  function toContentRating(comic) {
    const rating = contentPreferenceRating(comic);
    if (rating === "erotica" || rating === "pornographic") {
      return "ADULT" /* ADULT */;
    }
    if (rating === "suggestive") {
      return "MATURE" /* MATURE */;
    }
    return "EVERYONE" /* EVERYONE */;
  }
  function isComicAllowed(comic, preferences, restrictToPreferredLanguages = false) {
    if (preferences.contentRatings.length === 0 || preferences.types.length === 0 || !hasCoverUrl(comic.urlCover)) {
      return false;
    }
    if (restrictToPreferredLanguages && comic.translatedLanguage !== null && comic.translatedLanguage !== void 0 && !preferences.languages.includes(comic.translatedLanguage)) {
      return false;
    }
    if (comic.type !== null && comic.type !== void 0 && !preferences.types.includes(comic.type)) {
      return false;
    }
    if (!preferences.contentRatings.includes(contentPreferenceRating(comic))) {
      return false;
    }
    const excluded = /* @__PURE__ */ new Set([...preferences.excludedGenres, ...preferences.excludedFormats]);
    return ![...comic.genres ?? [], ...comic.tags ?? []].some((id) => excluded.has(id));
  }
  function toChapterId(chapter) {
    return sanitizeId(chapter.urlPath ?? `/comic/chapter/${chapter.id}`);
  }
  function chapterNumber(chapter) {
    const value = chapter?.chaNum ?? chapter?.serial;
    return typeof value === "number" && isFinite(value) ? value : void 0;
  }
  function formatChapter(chapter) {
    const number = chapterNumber(chapter) ?? /(?:chapter|ch\.?)[\s_]*(\d+(?:\.\d+)?)/i.exec(chapter?.dname ?? "")?.[1];
    if (number === void 0) {
      return void 0;
    }
    return `Ch. ${String(number).replace(/\.0$/, "")}`;
  }
  function dateFromTimestamp(value) {
    if (typeof value !== "number" || !isFinite(value) || value <= 0) {
      return void 0;
    }
    const date = new Date(value < 1e12 ? value * 1e3 : value);
    return isNaN(date.getTime()) ? void 0 : date;
  }
  function formatType(type) {
    return type !== null && type !== void 0 && type.length > 0 ? titleCase(type) : void 0;
  }
  function baseCard(node) {
    return {
      mangaId: sanitizeId(node.data.id),
      title: Application.decodeHTMLEntities(node.data.name),
      imageUrl: toAbsoluteUrl(node.data.urlCover)
    };
  }
  function cardSubtitle(comic) {
    return [formatChapter(comic.chapterNodes_last?.[0]?.data), formatType(comic.type)].filter((value) => value !== void 0).join(" \u2022 ") || void 0;
  }
  function toSearchResultItem(node) {
    return {
      ...baseCard(node),
      subtitle: cardSubtitle(node.data),
      contentRating: toContentRating(node.data)
    };
  }
  function toDiscoverItems(nodes, type) {
    return nodes.map((node) => toDiscoverItem(node, type)).filter((item) => item !== void 0);
  }
  function toDiscoverItem(node, type) {
    const chapter = node.data.chapterNodes_last?.[0]?.data;
    if (type === "featuredCarouselItem") {
      const chapters = node.data.chaps_normal ?? chapterNumber(chapter);
      const subtitle = [
        chapters !== void 0 ? `${chapters} chapters` : "",
        node.data.score_val !== null && node.data.score_val !== void 0 ? `\u2605 ${node.data.score_val.toFixed(2)}` : ""
      ].filter((part) => part.length > 0).join(" \u2022 ");
      return { type, ...baseCard(node), subtitle: subtitle || void 0 };
    }
    if (type === "chapterUpdatesCarouselItem") {
      if (chapter?.id === void 0) {
        return void 0;
      }
      return {
        type,
        ...baseCard(node),
        chapterId: toChapterId(chapter),
        subtitle: cardSubtitle(node.data),
        publishDate: dateFromTimestamp(chapter.datePublic ?? chapter.dateModify ?? chapter.dateCreate)
      };
    }
    return { type, ...baseCard(node), subtitle: cardSubtitle(node.data) };
  }
  function toLatestUploadNodes(result) {
    if (result === null || result === void 0 || !Array.isArray(result.items)) {
      throw new Error("The latest uploads could not be read.");
    }
    return result.items.flatMap(({ comic, chapters }) => {
      const data = comic?.data;
      const chapterNodes = chapters ?? [];
      if (data === void 0 || !hasCoverUrl(data.urlCover) || chapterNodes[0]?.data.id === void 0) {
        return [];
      }
      return [{ data: { ...data, chapterNodes_last: chapterNodes } }];
    });
  }
  function nodeNames(nodes) {
    return nodes?.map((node) => node?.data?.name?.trim()).filter((name) => name !== void 0 && name.length > 0).map((name) => Application.decodeHTMLEntities(name)) ?? [];
  }
  function stripHtml(html) {
    if (html === null || html === void 0 || html.length === 0) {
      return "";
    }
    const $ = Application.loadDocument(html);
    $("br").replaceWith("\n");
    $("p, div, li, blockquote").each((_, element) => {
      $(element).append("\n");
    });
    return Application.decodeHTMLEntities(
      $.root().text().replace(/\n{3,}/g, "\n\n").trim()
    );
  }
  function formatDateYmd(value) {
    if (value?.y === null || value?.y === void 0) {
      return void 0;
    }
    return [value.y, value.m?.toString().padStart(2, "0"), value.d?.toString().padStart(2, "0")].filter((part) => part !== void 0 && part !== null).join("-");
  }
  function toSourceManga2(node) {
    const comic = node.data;
    const authors = nodeNames(comic.authorNodes);
    const artists = nodeNames(comic.artistNodes).filter((artist) => !authors.includes(artist));
    const toTags = (values) => [...new Set(values.map(sanitizeId))].map((id) => ({
      id,
      title: TAG_TITLE_OVERRIDES[id] ?? titleCase(id)
    }));
    const tagGroups = [
      { id: "genres", title: "Genres", tags: toTags(comic.genres ?? []) },
      { id: "demographics", title: "Demographics", tags: toTags(comic.demographics ?? []) },
      { id: "tags", title: "Tags", tags: toTags(comic.tags ?? nodeNames(comic.tagNodes)) }
    ].filter((group) => group.tags.length > 0);
    const publicationFrom = formatDateYmd(comic.originalPubFrom);
    const publicationTill = formatDateYmd(comic.originalPubTill);
    const publishers = nodeNames(comic.publisherNodes);
    return {
      mangaId: sanitizeId(comic.id),
      mangaInfo: {
        primaryTitle: Application.decodeHTMLEntities(comic.name),
        secondaryTitles: (comic.altNames ?? []).map((title) => Application.decodeHTMLEntities(title)),
        thumbnailUrl: toAbsoluteUrl(comic.urlCover),
        synopsis: stripHtml(comic.summary?.html),
        author: authors.join(", ") || void 0,
        artist: artists.join(", ") || void 0,
        contentRating: toContentRating(comic),
        rating: typeof comic.score_val === "number" && isFinite(comic.score_val) ? Math.min(1, Math.max(0, comic.score_val / 10)) : void 0,
        status: titleCase(comic.originalStatus ?? comic.uploadStatus ?? "unknown"),
        tagGroups,
        additionalInfo: {
          ...comic.type ? { Type: titleCase(comic.type) } : {},
          ...comic.originalLanguage ? { "Original Language": comic.originalLanguage } : {},
          ...comic.translatedLanguage ? { [TRANSLATED_LANGUAGE_KEY]: comic.translatedLanguage } : {},
          ...publicationFrom !== void 0 ? {
            Publication: publicationTill !== void 0 ? `${publicationFrom} \u2013 ${publicationTill}` : publicationFrom
          } : {},
          ...comic.originalPubZone ? { Region: comic.originalPubZone } : {},
          ...typeof comic.chaps_normal === "number" ? { Chapters: String(comic.chaps_normal) } : {},
          ...typeof comic.follows === "number" ? { Follows: String(comic.follows) } : {},
          ...typeof comic.reviews === "number" ? { Reviews: String(comic.reviews) } : {},
          ...typeof comic.comments_total === "number" ? { Comments: String(comic.comments_total) } : {},
          ...publishers.length > 0 ? { Publishers: publishers.join(", ") } : {}
        }
      }
    };
  }
  function toChapter2(data, sourceManga) {
    const title = [data.dname?.trim(), data.title?.trim()].filter((value) => value !== void 0 && value.length > 0).filter((value, index, values) => index === 0 || value !== values[0]).map((value) => Application.decodeHTMLEntities(value)).join(": ");
    const sourceName = data.srcName?.trim();
    const profileNames = nodeNames(data.profileNodes);
    const scanlators = sourceName !== void 0 && sourceName.length > 0 ? [Application.decodeHTMLEntities(sourceName.charAt(0).toUpperCase() + sourceName.slice(1))] : profileNames.length > 0 ? profileNames : nodeNames(data.groupNodes);
    const uploader = data.userNode?.data?.name?.trim();
    const language = sourceManga.mangaInfo.additionalInfo?.[TRANSLATED_LANGUAGE_KEY];
    const langCode = typeof language === "string" && language.length > 0 ? language === "_t" ? "und" : language.replace(/_/g, "-") : "en";
    return {
      chapterId: toChapterId(data),
      sourceManga,
      chapNum: chapterNumber(data) ?? 0,
      volume: 0,
      title: title || void 0,
      langCode,
      publishDate: dateFromTimestamp(data.dateModify ?? data.dateCreate ?? data.datePublic),
      group: scanlators.join(", ") || (uploader !== void 0 ? Application.decodeHTMLEntities(uploader) : void 0)
    };
  }
  function parseChapterDetails(response, chapter) {
    const pages = (response.get_chapterNode?.data?.imageUrls ?? []).map(toAbsoluteUrl).filter((url) => url.length > 0);
    if (pages.length === 0) {
      throw new Error(`No pages were returned for chapter ${chapter.chapterId}.`);
    }
    return { id: chapter.chapterId, mangaId: chapter.sourceManga.mangaId, pages };
  }

  // src/XCOMIC/XCOMIC.ts
  var XCOMICInfo = sourceInfo({
    name: "XCOMIC",
    description: "Extension that pulls content from xcomic.me.",
    version: "1.0.0",
    icon: "icon.png",
    language: "English",
    websiteBaseURL: DOMAIN,
    contentRating: "ADULT" /* ADULT */,
    capabilities: ["chapters" /* CHAPTERS */, "homePage" /* HOME_PAGE */, "cloudflare" /* CLOUDFLARE */, "settings" /* SETTINGS */]
  });
  var XCOMIC = class extends PopmangoSource {
    constructor(cheerio) {
      super(cheerio, {
        domain: DOMAIN,
        settingsKeys: SETTINGS_KEYS,
        rateLimit: { numberOfRequests: 3, bufferInterval: 1, ignoreImages: true },
        interceptor: new XComicInterceptor()
      });
    }
    getMangaShareUrl(mangaId) {
      return `${DOMAIN}/comic/${mangaId}`;
    }
    async getSettingsSections() {
      const options = await this.getFilterOptions().catch(() => void 0);
      const sections = [
        {
          id: "content",
          header: "What to show",
          footer: "Titles outside these ratings and formats are left out everywhere.",
          rows: [
            selectRow("content_ratings", {
              label: "Content ratings",
              options: CONTENT_RATING_OPTIONS,
              multiple: true,
              get: () => this.preferences.contentRatings,
              set: (value) => this.settings.set(STATE_KEYS.CONTENT_RATINGS, value)
            }),
            selectRow("content_types", {
              label: "Formats",
              options: CONTENT_TYPE_OPTIONS,
              multiple: true,
              get: () => this.preferences.types,
              set: (value) => this.settings.set(STATE_KEYS.CONTENT_TYPES, value)
            }),
            selectRow("languages", {
              label: "Languages",
              options: LANGUAGE_OPTIONS,
              multiple: true,
              get: () => this.preferences.languages,
              set: (value) => this.settings.set(STATE_KEYS.LANGUAGES, value)
            })
          ]
        },
        {
          id: "sections",
          header: "Home page",
          footer: "Choose which sections appear. Leave everything unticked to show them all.",
          rows: [
            selectRow("visible_sections", {
              label: "Sections shown",
              options: SECTION_OPTIONS,
              multiple: true,
              get: () => this.visibleSections,
              set: (value) => this.settings.set(STATE_KEYS.VISIBLE_SECTIONS, value)
            })
          ]
        }
      ];
      const excluded = {
        id: "excluded",
        header: "Never show",
        footer: "A title carrying any of these is left out of every list.",
        rows: [
          selectRow("excluded_formats", {
            label: "Excluded formats",
            options: FORMAT_OPTIONS,
            multiple: true,
            get: () => this.preferences.excludedFormats,
            set: (value) => this.settings.set(STATE_KEYS.EXCLUDED_FORMATS, value)
          })
        ]
      };
      if (options !== void 0 && options.genres.length > 0) {
        excluded.rows.unshift(
          selectRow("excluded_genres", {
            label: "Excluded genres",
            options: options.genres.map((genre) => ({ id: genre.id, title: genre.title })),
            multiple: true,
            get: () => this.preferences.excludedGenres,
            set: (value) => this.settings.set(STATE_KEYS.EXCLUDED_GENRES, value)
          })
        );
      }
      sections.push(excluded);
      return sections;
    }
    async getDiscoverSections() {
      const visible = this.visibleSections;
      const wanted = visible.length > 0 ? new Set(visible) : void 0;
      return SECTION_IDS.filter((id) => wanted === void 0 || wanted.has(id)).map(
        (id) => DISCOVER_SECTIONS[id]
      );
    }
    async getDiscoverSectionItems(section, metadata) {
      const page = metadata;
      switch (section.id) {
        case SECTIONS.TOP_RATED: {
          const current = page?.page ?? 1;
          const result = await this.browse(current, "field_score", "", /* @__PURE__ */ new Map(), /* @__PURE__ */ new Map(), void 0);
          return {
            items: toDiscoverItems(result.nodes, "featuredCarouselItem"),
            metadata: result.nextPage !== void 0 ? { page: result.nextPage } : void 0
          };
        }
        case SECTIONS.LATEST_UPLOADS: {
          const result = await this.latestUploads(page?.before);
          return {
            items: toDiscoverItems(result.nodes, "chapterUpdatesCarouselItem"),
            metadata: result.before !== void 0 ? { before: result.before } : void 0
          };
        }
        case SECTIONS.RECENTLY_ADDED: {
          const preferences = this.preferences;
          const nodes = (await fetchRecentlyAdded()).get_comic_recentlyAdded?.items ?? [];
          return {
            items: toDiscoverItems(
              nodes.filter((node) => isComicAllowed(node.data, preferences, true)),
              "simpleCarouselItem"
            )
          };
        }
        default:
          return { items: [] };
      }
    }
    /**
     * Offers the site's filters as tag sections.
     *
     * 0.8 has no separate sort control, so the sort order is a section of tags
     * as well; picking more than one leaves the first in effect.
     */
    async getFilterSections() {
      const options = await this.getFilterOptions().catch(() => void 0);
      const sections = [
        {
          id: FILTERS.SORT,
          title: "Sort by",
          tags: SORT_OPTIONS.map((option) => filterTag(FILTERS.SORT, option.id, option.title))
        },
        {
          id: FILTERS.VIEWS,
          title: "View charts",
          tags: MOST_VIEWS_OPTIONS.map((option) => filterTag(FILTERS.VIEWS, option.id, option.title))
        }
      ];
      if (options !== void 0 && options.genres.length > 0) {
        sections.push({
          id: FILTERS.GENRE,
          title: "Genres",
          tags: options.genres.map((genre) => filterTag(FILTERS.GENRE, genre.id, genre.title))
        });
      }
      sections.push({
        id: FILTERS.FORMAT,
        title: "Formats",
        tags: FORMAT_OPTIONS.map((option) => filterTag(FILTERS.FORMAT, option.id, option.title))
      });
      if (options !== void 0 && options.demographics.length > 0) {
        sections.push({
          id: FILTERS.DEMOGRAPHIC,
          title: "Demographics",
          tags: options.demographics.map(
            (entry) => filterTag(FILTERS.DEMOGRAPHIC, entry.id, entry.title)
          )
        });
      }
      sections.push(
        {
          id: FILTERS.INC_MODE,
          title: "Included genre matching",
          tags: MODE_OPTIONS.map((option) => filterTag(FILTERS.INC_MODE, option.id, option.title))
        },
        {
          id: FILTERS.EXC_MODE,
          title: "Excluded genre matching",
          tags: MODE_OPTIONS.map((option) => filterTag(FILTERS.EXC_MODE, option.id, option.title))
        },
        {
          id: FILTERS.ORIG_STATUS,
          title: "Original status",
          tags: STATUS_OPTIONS.map((option) => filterTag(FILTERS.ORIG_STATUS, option.id, option.title))
        },
        {
          id: FILTERS.SITE_STATUS,
          title: "Upload status",
          tags: STATUS_OPTIONS.map((option) => filterTag(FILTERS.SITE_STATUS, option.id, option.title))
        },
        {
          id: FILTERS.CHAPTERS,
          title: "Chapter count",
          tags: CHAPTER_COUNT_OPTIONS.map(
            (option) => filterTag(FILTERS.CHAPTERS, option.id, option.title)
          )
        },
        {
          id: FILTERS.TLANG,
          title: "Translated language",
          tags: LANGUAGE_OPTIONS.map((option) => filterTag(FILTERS.TLANG, option.id, option.title))
        },
        {
          id: FILTERS.OLANG,
          title: "Original language",
          tags: LANGUAGE_OPTIONS.map((option) => filterTag(FILTERS.OLANG, option.id, option.title))
        }
      );
      return sections;
    }
    /** The release year is a number, so it gets a box rather than a tag. */
    async getSearchFieldList() {
      return [{ id: YEAR_FIELD, name: "Release year", placeholder: "e.g. 2019 or 2015-2019" }];
    }
    /** The browse query takes an explicit exclusion list of its own. */
    async supportsTagExclusion() {
      return true;
    }
    async getSearchResultItems(query, metadata) {
      const title = (query.title ?? "").trim();
      const included = groupTags(query.includedTags);
      const excluded = groupTags(query.excludedTags);
      const pasted = await this.resolvePastedUrl(title, excluded);
      if (pasted !== void 0) {
        return pasted;
      }
      const page = metadata?.page ?? 1;
      const sortBy = included.get(FILTERS.VIEWS)?.[0] ?? included.get(FILTERS.SORT)?.[0] ?? "field_score";
      const result = await this.browse(page, sortBy, title, included, excluded, yearField(query));
      return {
        items: result.nodes.map(toSearchResultItem),
        metadata: result.nextPage !== void 0 ? { page: result.nextPage } : void 0
      };
    }
    async getMangaInfo(mangaId) {
      const response = await fetchComic(mangaId);
      if (response.get_comicNode === null || response.get_comicNode === void 0) {
        throw new Error(`No series was found for ${mangaId}.`);
      }
      return toSourceManga2(response.get_comicNode);
    }
    async getChapterList(sourceManga) {
      const first = await fetchChapters(sourceManga.mangaId, 1);
      const firstResult = first.get_comic_chapterList_uniqList;
      if (firstResult === null || firstResult === void 0) {
        return [];
      }
      const pageCount = firstResult.paging?.pages ?? 1;
      const rest = await Promise.all(
        Array.from(
          { length: Math.max(0, pageCount - 1) },
          (_, index) => fetchChapters(sourceManga.mangaId, index + 2)
        )
      );
      return [first, ...rest].flatMap(
        (response) => (response.get_comic_chapterList_uniqList?.items ?? []).filter((item) => item.data.dbStatus === "normal").map((item) => toChapter2(item.data, sourceManga))
      );
    }
    async getPages(chapter) {
      const id = chapter.chapterId.split("/").pop()?.split("-")[0];
      if (id === void 0 || id.length === 0) {
        throw new Error(`Cannot work out a chapter id from ${chapter.chapterId}.`);
      }
      return parseChapterDetails(await fetchChapterPages(id), chapter);
    }
    // -----------------------------------------------------------------
    // Internals
    // -----------------------------------------------------------------
    /** What the reader has chosen to see, with the site's defaults behind it. */
    get preferences() {
      return {
        contentRatings: choices(
          this.settings.stringArray(STATE_KEYS.CONTENT_RATINGS),
          CONTENT_RATING_OPTIONS.map((option) => option.id),
          DEFAULT_CONTENT_RATINGS
        ),
        types: choices(
          this.settings.stringArray(STATE_KEYS.CONTENT_TYPES),
          CONTENT_TYPE_OPTIONS.map((option) => option.id),
          DEFAULT_CONTENT_TYPES
        ),
        languages: (() => {
          const chosen = this.settings.stringArray(STATE_KEYS.LANGUAGES);
          return chosen.length > 0 ? chosen : DEFAULT_LANGUAGES;
        })(),
        excludedGenres: this.settings.stringArray(STATE_KEYS.EXCLUDED_GENRES),
        excludedFormats: this.settings.stringArray(STATE_KEYS.EXCLUDED_FORMATS)
      };
    }
    get visibleSections() {
      return this.settings.stringArray(STATE_KEYS.VISIBLE_SECTIONS, new Set(SECTION_IDS));
    }
    getFilterOptions() {
      this.filterOptions ??= fetchSearchPage().then(parseFilterOptions).catch((error) => {
        this.filterOptions = void 0;
        throw error;
      });
      return this.filterOptions;
    }
    /**
     * The reader's settings, plus anything the search screen adds on top.
     *
     * A filter chosen for one search narrows that search only; it never
     * loosens what the settings screen has ruled out.
     */
    effectivePreferences(excluded) {
      const preferences = this.preferences;
      return {
        ...preferences,
        excludedGenres: [...preferences.excludedGenres, ...excluded.get(FILTERS.GENRE) ?? []],
        excludedFormats: [...preferences.excludedFormats, ...excluded.get(FILTERS.FORMAT) ?? []]
      };
    }
    async browse(page, sortBy, word, included, excluded, year) {
      const preferences = this.effectivePreferences(excluded);
      const select = buildBrowseSelect(page, sortBy, word, included, preferences, year);
      const nodes = (await fetchBrowse(select)).get_comic_browse_items ?? [];
      return {
        nodes: nodes.filter((node) => isComicAllowed(node.data, preferences)),
        // The query has no total, so a full page is taken to mean another.
        nextPage: nodes.length === PAGE_SIZE ? page + 1 : void 0
      };
    }
    /**
     * Walks the upload feed until a page survives filtering.
     *
     * The feed carries every language and format at once and cannot be
     * narrowed by the site, so a page can filter down to nothing.
     */
    async latestUploads(before) {
      const preferences = this.preferences;
      const nodes = [];
      const seen = /* @__PURE__ */ new Set();
      let cursor = before;
      for (let attempt = 0; attempt < MAX_LATEST_REQUESTS && nodes.length === 0; attempt += 1) {
        const result = (await fetchLatestUploads(cursor)).get_comic_latestUploads;
        for (const node of toLatestUploadNodes(result)) {
          if (seen.has(node.data.id) || !isComicAllowed(node.data, preferences, true)) {
            continue;
          }
          seen.add(node.data.id);
          nodes.push(node);
        }
        cursor = typeof result?.before === "number" && isFinite(result.before) ? result.before : void 0;
        if (cursor === void 0) {
          break;
        }
      }
      return { nodes, before: nodes.length > 0 ? cursor : void 0 };
    }
    /** Turns a pasted series URL into a single result. */
    async resolvePastedUrl(title, excluded) {
      const id = /^https?:\/\/(?:www\.)?xcomic\.(?:me|net)\/comic\/([a-zA-Z0-9]+)/i.exec(title)?.[1];
      if (id === void 0) {
        return void 0;
      }
      const response = await fetchComic(id);
      if (response.get_comicNode === null || response.get_comicNode === void 0) {
        return void 0;
      }
      if (!isComicAllowed(response.get_comicNode.data, this.effectivePreferences(excluded))) {
        return { items: [] };
      }
      return { items: [toSearchResultItem(response.get_comicNode)] };
    }
  };
  function buildBrowseSelect(page, sortBy, word, included, preferences, year) {
    const includedTagIds = [...included.get(FILTERS.GENRE) ?? [], ...included.get(FILTERS.FORMAT) ?? []];
    const excludedTagIds = [...preferences.excludedGenres, ...preferences.excludedFormats];
    const { min, max } = yearRange(year);
    const translated = included.get(FILTERS.TLANG);
    return {
      where: "browse",
      page,
      size: PAGE_SIZE,
      init: (page - 1) * PAGE_SIZE,
      sortby: sortBy,
      word,
      incOLangs: included.get(FILTERS.OLANG) ?? [],
      incTLangs: translated !== void 0 && translated.length > 0 ? translated : preferences.languages,
      incGenres: [...new Set(includedTagIds)],
      excGenres: [...new Set(excludedTagIds)],
      incGenresMode: included.get(FILTERS.INC_MODE)?.[0] ?? "and",
      excGenresMode: included.get(FILTERS.EXC_MODE)?.[0] ?? "or",
      incTypes: preferences.types,
      incDemographics: included.get(FILTERS.DEMOGRAPHIC) ?? [],
      incContentRatings: preferences.contentRatings,
      releaseYearMin: min,
      releaseYearMax: max,
      origStatus: included.get(FILTERS.ORIG_STATUS)?.[0] ?? null,
      siteStatus: included.get(FILTERS.SITE_STATUS)?.[0] ?? null,
      chapCount: included.get(FILTERS.CHAPTERS)?.[0] ?? "",
      ignoreGlobalULangs: true,
      ignoreGlobalGenres: true,
      ignoreGlobalBlocks: true
    };
  }
  function yearRange(year) {
    const value = (year ?? "").trim();
    if (value.length === 0) {
      return { min: null, max: null };
    }
    if (value.includes("-")) {
      const [from, to] = value.split("-").map((part) => Number(part) || null);
      if (from !== null && from !== void 0 && to !== null && to !== void 0) {
        return { min: Math.min(from, to), max: Math.max(from, to) };
      }
      const single2 = from ?? to ?? null;
      return { min: single2, max: single2 };
    }
    const single = Number(value) || null;
    return { min: single, max: single };
  }
  function yearField(query) {
    const value = query.parameters[YEAR_FIELD];
    return typeof value === "string" && value.trim().length > 0 ? value.trim() : void 0;
  }
  function choices(stored, allowed, fallback) {
    const kept = stored.filter((value) => allowed.includes(value));
    return kept.length > 0 ? kept : fallback;
  }
  function groupTags(tags) {
    const chosen = /* @__PURE__ */ new Map();
    for (const tag of tags) {
      const split = splitFilterTag(tag.id);
      if (split === void 0) {
        continue;
      }
      chosen.set(split.section, [...chosen.get(split.section) ?? [], split.value]);
    }
    return chosen;
  }
  return __toCommonJS(XCOMIC_exports);
})();
this.Sources = _Sources; if (typeof exports === 'object' && typeof module !== 'undefined') {module.exports.Sources = this.Sources;}
